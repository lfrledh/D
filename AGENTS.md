# D：智能体入口

现行规则 C2026-09-23.1；产品方向 P2026-09-23.1。规则整理不修改既有任务的失败、剩余预算或已验证版本。

## 先定位，再按需阅读

1. 核对真实 Git 根、分支、HEAD、索引、个人改动和已知任务。项目名相同不等于同一仓库；外盘未挂载时停止相关写入，不建伪目录、不改存内盘。
2. 读 [当前行动](docs/CURRENT_ACTIONS.zh-CN.md)：源、应用、候选、当前授权、保护与下一步。只信聊天摘要或旧“下一阶段”会走错基线。
3. 产品决定读 [原则](docs/PRODUCT_PRINCIPLES.zh-CN.md) 和 [目标](docs/PRODUCT_GOALS.zh-CN.md) 相关行；定位代码/测试读 [实际导航](docs/REPOSITORY_MAP.zh-CN.md)。执行任务再读该任务规格及相关接口，不默认全读历史。
4. 派工、权限事件、预算、集成和恢复按 [协作规程](docs/MULTI_AGENT_WORKFLOW.zh-CN.md)，规格用 [单一模板](docs/TASK_SPEC_TEMPLATE.md)。本人操作读 [集中待办](docs/FAILURE_AND_PERMISSION_AUDIT.zh-CN.md)。

## 当前方向与实施边界

D 是面向创作者、艺术家和学生、兼顾初学者的本地 AI **统一节点工作台**。场景是能力组合例子；模态用于类型、筛选和专业编辑，不是强制工作流分界。节点显示执行来源、输入输出与必选/可选条件，端口/连接的数据可检查；不暴露每个张量操作。旧模态导航仍是代码现状，新节点 UI 和具体架构未由文档自动实现。

模型要求、适配支持、实测配置、硬件建议、用户选择分开；16 GiB 开发机不是产品上限。不得静默忽略条件、降精度、换模型或转云端。GPU/CPU/NPU按真实支持与有效性能选择，当前重神经网络GPU优先、CPU显式备选；细则见[设备原则](docs/EXECUTION_DEVICE_POLICY.zh-CN.md)。

当前获准实施为D-NODE-CATALOG-01模型节点说明原型：保留四模态页，展示已适配模型/引擎/端口/参数与用户标签，不做组合。上批S0—S5已结案，S6组合提案本轮不实施。已有 AP1/I2V/CORE 候选保留，不自动续跑、集成或重构。正式 App 不附模型权重，由用户显式下载/导入并处理自己的使用资格；D仍负责自身代码、运行时、来源校验和实际分发行为，不能用资格框创造许可。实现缺口见支持表。

## 不可破坏的工程边界

- `DInference` 仅值型契约，不依赖运行时、MLX、网络或UI；`DRuntime`仅依赖它与标准库/Foundation。`DWorkbench`拥有项目、资产、安装与应用任务，UI负责展示/原生交互，App注入具体backend/runtime。服务不泄露MLXArray、ChatSession、NSImage、窗口或书签。
- 共享契约、存储、工程装配由Lead单一协调。actor的await可重入，整次重推理需要显式所有权；不得批量添加unsafe/nonisolated/unchecked Sendable掩盖并发问题。
- execute返回前drain，release完成前不得启动下一重任务；文本/图像共享进程级MLX许可。取消状态反映真实停止和清理。仅在后端空闲/shutdown后清理本实例跟踪的未发布文件，不扫描或删除作品。
- 项目、文档、资产、任务、模型身份、安装实例分开；提交后输入不可变。排队任务持有安装使用权，释放前不得迁移/删除。下载、校验、操作兼容与资源预算不混为“可运行”。
- 原件/候选/采用/派生/预览分开；保存失败不得破坏已有作品，未知文件不猜删。外盘失联/空间不足明确报错。媒体用资源引用，未知位深/色彩保留未知，PNG不冒充RAW。
- 导出仅使用目标授权，经系统同卷临时目录准备、原子不覆盖发布；不能假定可在目标目录创建兄弟临时文件。发布后同步失败仍保留结果。迁移先备份原清单、不改媒体；界面、项目schema、内部接口、未来外部协议独立演进。
- 依赖改进在获准范围内记录问题、固定源码、许可、补丁、回退及验证；不只改SwiftPM checkout。Vendor局部规则继续有效。旧Packages是普通源码目录，不新建嵌套仓库；来源见历史索引。不为未来造空包或绕开保存/调度的生成路径。

## 授权、协作、证据

已批准阶段内普通查证、澄清、派工、预算内修复、审核、工作分支提交/推送自主完成；阶段结束交付成果和下一有限提案，下一产品阶段等批准。本阶段不需要下载或模型运行；按影响允许局部CPU、独立构建与隔离GUI验收。长期开发材料授权仍覆盖已批准阶段的必要固定版本下载/外盘独立依赖环境；Lead核对来源、许可、摘要、空间和资源，不逐项重复问。它不代第三方授予权利、不允许未知费用、任意对外消息、上传用户资产、改系统权限或发布。

项目不设固定Worker人数上限；按就绪度、耦合、实际工具额度和审核吞吐分配，禁止递归。写Worker必须逐次核验受限链路、独立工作树/分支/索引/写根和可观察模型设置；不能将全访问继承当隔离。只读调查可读固定快照，不据此宣称写隔离通过。普通初交＋最多两轮修复，再最多一次有界Lead接管；旧任务预算不刷新。重要Lead实现由非实现者检查。

Mac默认由Lead协调，无需重复问空闲；用户使用时让出。重构建/GPU/GUI串行，保持独立产物/项目/偏好，保护未保存数据，不强杀未知进程或覆盖普通D。锁屏只阻塞相应GUI，所需本人点击/设备/试听进集中清单，返回时集中处理、可用已授权短音效提醒；不轮询位置或反复催解锁。

仅显式暂存获准文件；保护个人scheme的内容、摘要、索引与未暂存状态。保留历史整合，源只快进接纳已验组合；不reset/restore/stash/清理/rebase/cherry-pick/squash/amend、强推或推进main。沿用人类Git作者配置，提交说明标注Codex工作和task ID。

构建、CPU夹具、真实模型、原生UI、真人、组合与交付分别报告；跳过/超时/读取旧证据不等于本轮通过。XCTest可能修改宿主签名，后续普通沙盒验收须重建核对；禁签名构建仅证明编译。大日志/样本留工作树之外的外盘证据区，任务记录留脱敏索引、版本、失败、保护与恢复点。未测试的新路径不默认替换已有行为。

旧规则的去向与明确替代见[本轮规则表](docs/tasks/D-CONTEXT-RESET-01.md#规则去向)；历史不是当前开工指令。
