# 受控音乐与哼唱路线

## 2026-09-13 条件器乐有限出口已完成；下一目标为歌词演唱

MRT2已进入普通沙盒内嵌引擎工作台：音符/同起点和弦条件、风格、seed与短片段→新的48kHz混音候选→试听比较/采用拒绝→条件和实际参数保存重开/不覆盖WAV导出。当前small固定profile最多16秒、40ms格；不是所有Mac容量或未来后端上限。真实短旋律修改/取消恢复/与SA3交替以及持久化证据见[本阶段结案](tasks/D-MRT2-WORKBENCH-01.md#2026-09-13-阶段验收与本地接纳)。用户听到正常钢琴但有类似沙锤背景声；声音服从近似，严格和弦、纯配器、分轨和局部区外音频锁定仍未完成。

下一有界产品提案：**一段旋律＋歌词→专门歌声候选→局部改词/音高/时值后新候选→试听比较/保存导出**。先只读核实一个Mac可运行的后端、明确一种语言、合法声库/vocoder、输入音素/时值和输出采样率；再冻结短句、本机资源与控制/取消/原件保护验收。DiffSinger等仍是候选，不把TTS或器乐MRT2当演唱，不自动下载声库/启动研发。HUM短单声部文件转音符可在独立接口就绪后另包推进，录音不作为文件入口前置；H09设备单列。完整谱面编辑、所有文字增强和META不构成下一音乐入口的前置。下方按日期保留历史计划。

## 2026-09-13 下一阶段范围已规划

当前[短旋律条件工作台计划](tasks/D-MRT2-WORKBENCH-01.md)把下方提案细化为一次有限产品闭环：简明音符/和弦条件与风格输入→新器乐候选→试听比较、采用拒绝→条件/实际运行快照安全保存重开与WAV导出。先完成固定引擎的应用部署、结构化参数族与44.1/48kHz兼容契约，再按依赖分派后端、条件数据、局部UI三个工作包；人数按就绪度安排，真实模型/GUI排队。状态是规划就绪，产品实现/部署未开始，不能把独立probe实验升级为App通过。

首版明确40ms条件时间格和近似声音服从，修改音符产生新完整候选；不宣称区外音频锁定、不把音符降为普通prompt。现有small短片段profile不是所有Mac/未来引擎的容量上限，SA3/图文精度不变。条件闭环之后紧接旋律+歌词演唱候选，另核声库/vocoder许可；HUM录后转音符可在独立数据边界就绪后推进，麦克风H09和听感H17继续按现有清单集中办理。下面各日期记录保留当时状态。

## 2026-09-13：短音符条件后端已实验验证，正式接线为下一提案

用户已批准固定MRT2 small约2.97GB、独立依赖和本地实验，实际来源与结论见[任务记录](tasks/D-MRT2-CONDITIONS-01.md)。最终11个独立进程的短旋律、和弦、空/缺省条件、局部条件变更、不同seed、取消恢复及raw对照按预期完成；其中10个4秒48kHz双声道float32 WAV与1个取消，无新GUI或真人听感结论。输入时间基25Hz，0关/1持续/2起音按明确音高送入；输出是混音波形，不能从WAV推出可独立编辑的各声部或精确谱面。

实际边界：单旋律样例主频对应C/E/G/C，修改第二音后候选改变；后面的声音也会改变。和弦样例出现目标外音高，全零音符条件不等于保证静音。AI修改应先成为新候选，原作品/条件仍保留；“锁定作品数据”不能宣称“锁定音频每个采样”。原始和导出两路径均有上游int16转换，导出图内部精度仍unknown；不以float32容器宣称全FP32。小规模结果不能代替专业听感、复杂和声或所有Mac验证。

建议下一阶段有界出口：把这一后端接入既有AudioRequest/DRuntime与音频候选工作台，先支持明确短旋律条件文件/简明音符输入、风格提示、seed和时长；提交冻结条件/模型版本，生成/取消释放、试听比较/采用拒绝、安全保存重开和导出。用能力表显示近似控制；未知输入拒绝，不静默降为普通prompt。每任务随机state独立，SDK版本/结构不兼容明确报错。依赖普通沙盒部署与真人样本质量门槛，未过不默认启用产品入口；沿用SA3原路径而不改其精度。

不在本轮实现完整乐谱排版、DAW、实时神经监听或声部分轨。下一正式条件闭环之后紧接专门歌词演唱候选；声库/vocoder另核许可与资源。HUM文件转录可并行于独立工作，不等所有文字增强或META；H09设备验收另列。MRT2 base/其他导出仅登记未来配置，不因本机small实测将产品容量锁死；新型号与依赖需具体授权和真实验证。

## 2026-09-12：APP1 文件输入出口已完成；下一条件控制切片待批准

[APP1](tasks/D-AUDIO-APP-01.md)已将普通沙盒内嵌SA3 small接入音频工作台，真实完成提示/兼容WAV参考/帧范围重绘、取消交接、试听比较/采用拒绝、保存重开与不覆盖导出。当前是混音波形候选，参考服从近似；不输出可编辑MIDI，也不支持精确歌词/歌声或逐音符服从。8次真实任务为6完成、2取消，不与历史测试加总；麦克风仍后置。

下一产品目标提案：**让创作者提供短旋律/和声条件，让AI生成可比较的器乐候选**。先依据已研究的MRT2 small候选核实固定输入输出、许可、Mac依赖与资源，再冻结一个短片段的条件表达/锁定语义与实测标准；新权重/依赖仍需明确授权。源码支持不是本机通过，精确/近似/不支持分项声明。不先造完整乐谱编辑器；条件可先由明确的文件/简明控件进入后端，界面只展开真实能力。下一里程碑紧接独立歌声候选（DiffSinger等仍待声库/vocoder许可及本机验证），TTS不替代。

HUM录后转音符可按已有短单声部文件另开独立切片，不依赖设备实时录音或全部文本高级功能；移动捕捉、更多容器/来源和高配模型矩阵保留原路线。H09等设备，CAF→模型输入只允许显式派生转换，不改原件。本轮到APP1提交/推送检查点停止，不自动启动上述研究、模型下载或研发。下面各日期的提案/未接线状态保留历史。

## 2026-09-11：麦克风保留，下一阶段为文件输入的 AI 音频闭环（提案）

状态分开：用户已要求保留 H09、设备就绪再测，并批准本轮规划；下列实现范围等待审阅，本轮未启动开发。核验源基线为 `79f965096be61cbc6460d5867e179be85df084d9`；NAV2 已验收并推送，实际受测 `bbfc72d3d4606ca96dc3c53b08fd59a602796f6f`，其后四份结案文档不改变产品代码。证据为 `D-Development/AgentTrials/D-UI-HIERARCHY-01/run-20260911T110719Z-human-return/final-receipt.json`。以下旧日期的待接纳/未出声状态只代表当时事实。

### 已核实起点与有限终点

SA3 small 后端的真实提示生成、参考变体、局部重绘和取消已有验收；AW1 服务和新导航已进入源，音频 UI 仍受隔离门控制。`D/AppSessionFactory.swift:makeAudioBackend` 仅在 DEBUG 隔离会话并显式提供 `D_AUDIO_BACKEND_CONFIGURATION` 时接入 Python/provider，普通启动返回无音频后端。H15 不是再给一次权限就可关闭，也不能以命令行出声代替普通应用验收。

建议终点：在保持现有沙盒和签名方案的独立普通构建中，通过音频模态完成“提示词或已导入音频 → 新候选/参考变体/圈段重绘 → 试听比较 → 采用或拒绝 → 安全保存重开和导出”。不要求拥有麦克风；不宣称 AW1 含录音的全部原始验收已完成。

### 工作顺序与边界

|顺序|有界成果|依赖和验收出口|
|---|---|---|
|1 引擎接线|Lead 先确定既有本地解释器、provider、模型及缓存/产物的受控访问、生命周期和恢复契约；以最小普通沙盒检查确认可行，再制作明确的引擎就绪/缺失/不可访问状态与配置入口。用户不必在终端手填测试环境变量。|复用已授权固定环境/权重，检查子进程实际权限和依赖发现；不扫描全盘、安装新依赖、关沙盒或改签名。如果现有边界不足，保留具体阻塞与可审阅替代方案，不先铺满 UI。完整安装器和商业发行包不在本阶段。|
|2 接通三种生成|复用 AudioRequest、DRuntime、ProjectAudioController 和现有候选存储，完成提示生成、参考变体、明确帧区间重绘的应用装配。|先支持后端已兼容的 44.1kHz 双声道 WAV；不兼容的 CAF/采样率/通道明确拒绝并解释。不把显式转换或录音设为文件输入闭环前置。|
|3 用户完成作品|沿用“项目→音频→创作/参数/资产”，补真实引擎状态、可信进度/取消、原件与候选试听比较、采用拒绝、保存重开、安全导出。|复用 NAV2/AW1，不重写导航或存储。使用实际提交快照保存来源；故障后保留原件、既有作品及可恢复产物。|
|4 组合与真实验收|按影响执行 CPU/组件回归、独立普通构建、同一项目的真实三操作、取消与恢复、候选处理和退出重开，最后集成已验收具体 SHA。|GPU/GUI 由 Lead 串行持有；到时核对资源窗口。普通 D 与用户作品保持；缺少真实路径证据时保留隔离门，不只凭 mock 或无签名编译默认启用。|

### 派工前冻结的行为与反例

- 提交绑定项目、文档版本、源资产摘要、帧范围和实际参数；修改提示或切换文档不改变在途请求。迟到结果只进入原任务归属，不自动采用到新文档。
- 原件直到采用前始终不变；采用更改当前作品引用，拒绝不删除原件/已交付媒体，可返回原件。生成错误、取消和保存失败均不能破坏已保存状态。
- “正在取消”持续到子进程停止和资源释放；下一重任务不得提前运行。解释器缺失、配置失效、外盘不可访问需与模型执行失败区分。
- 保存重开恢复候选、采用状态、来源和执行参数；导出拒绝覆盖，发布前后校验有效媒体。产物已生成但项目保存失败时，明确显示可恢复状态，不误报保存成功。
- 首测沿用 SA3 small 6秒、8步、guidance 1、seed 42；这是 M4/16GiB 验收配置，不是所有 Mac 的产品上限。精度不降，高配/长片段逐配置登记“已声明/已适配/已实测”，未知不标通过。

### 分工、验收与预算

Lead 负责引擎部署取舍、公共契约、AppSessionFactory、ProjectSession/ProjectStore 共享装配、资源排队、组合审核及接纳。契约成立后，按实际文件所有权安排 0—2 个受限 CLI Worker：一包实现引擎配置/状态的局部服务及 CPU 检查，一包完成只依赖冻结状态接口的音频交互及局部检查；不能共同修改共享装配。部署探针未过或第二包不独立时串行，不为并行新增框架。普通清晰实现优先 Terra/medium，安全/生命周期复杂部分可按既有规程选择 Sol，模型路由须核验。

这是已有 AW1 闭环的后续工作提案，不重开旧 VIEW/H09 失败额度。执行前明确哪些是新接线范围、哪些复用已验收修补；旧失败和来源记录保留。新包按现行初交+两轮修复及一次有界 Lead 接管规则；Lead 的重要实现仍需非实现者审查，不靠增加轮次掩盖部署阻塞。

机器验收：配置/缺失/输入保护、晚到结果、取消交接、候选决策、保存失败与重开、导出不覆盖；真实 WAV 可解码且参数/帧数/摘要匹配，重绘区外按已有 PCM 标准保持。人工试听：可播放、无明显爆音/断续、比较和局部效果可理解；听感不能替代数值/文件检查，合法 WAV 也不能替代听感。既有 CLI 历史证据不记作本次 App 通过；记录受测代码、普通构建、实际模型任务、GUI 和未执行项。

### H09 与之后的路线

用户明确暂时没有麦克风，H09 为“后置验收／等待设备”，不再等待本轮回复，不重复探测或制造系统弹窗。以后每次用户回到 Mac 并要求集中办理授权时，只提醒一次“设备是否已到，是否本次补录音验收”；仍无设备则保持状态并办理其他事项。设备到位后独立完成短录音、停止、试听、保存重开；如需把 48kHz 单声道 CAF 用作模型参考，另冻结显式派生转换契约并验证，不偷偷重采样或改原件。H15 的文件输入子闭环可以独立关闭，录音输入链仍留明示限制。

下一产品提案在本阶段出口后，按后端能力验证“更可控的音乐修改/创作意图”，并紧接专门歌声候选；HUM 录后转音符可先以已有短单声部文件规划，实时捕捉与手机伴侣等待对应设备/阶段。精确谱面、歌词演唱需新能力和许可验证，不能由当前 SA3 宣称支持。TTS、长音频/高配矩阵、格式扩展、资产来源增强分别保留，不以完整 DAW、图像/文本高级功能全部完成为前置。本轮不下载模型、不执行音乐研究原型或下一实施包。

## 2026-09-10：后端有限出口已通过

SA3 small music的提示生成、参考变体、区间重绘及真实取消/失败恢复已验收并本地接纳；6秒WAV样本已获用户正常试听确认。此前“许可未确认/尚未出声”只作历史。当前模型仍不支持精确乐谱、MIDI、和声或歌词演唱控制；不能把它们说成已完成。下一用户价值出口是“提示/参考/圈段 → 试听候选 → 比较采用 → 安全保存导出”，复用既有音频候选服务。H09录音创建/写入保护缺口独立待修，不阻塞文件输入后端，也不默认接纳未验收schema4。HUM转录和专门歌声继续保留明确后续位置，完整DAW/文字增强不是前置；本轮不启动下一批。

## 2026-09-09：用户改为后端能力优先（当前有效）

用户已批准 D-AUDIO-BACKEND-01：先完成模型驱动的音频后端，再依据真实输入/输出决定编辑交互。谱面、和声、歌词等是可能的模型条件，不以先建传统 DAW、完整乐谱编辑器或 MUS-0 为所有后端的前置。下文此前 MUS-0→MUS-1 顺序保留为历史，本节覆盖这个依赖关系。录音/原声保全仍有价值，但 H09/H10 不阻塞文件输入后端的 CPU 工作。

本批首个实现候选是固定源码 SA3 MLX：文字生成、参考音频变体、区间重绘；输出44.1kHz双声道float32 WAV主文件与执行快照。参考服从为近似；重绘区外按源PCM转float32后的值保留，原始文件不变。首版不做接缝淡化，不宣称逐音符/歌词/和弦的精确控制，更不把任意谱面静默降成普通文字。没有MIDI/歌声控制的模型须明确不支持。2026-09-10检查点：音频接口/跨语言三操作CPU与组合编译通过；模型真实验收待权重使用许可及独立依赖环境。资源窗口本轮已确认并完成图文回归，当前音频仍不能记作已出声。

SA3源码为MIT，模型另受Stability Community与Gemma条款；已请求用户确认适用商业登记及许可，未代填/接受/下载。ACE-Step保留替代研究，但固定版本Mac路径保留PyTorch FP32及MLX FP32副本，16GiB不能套用CUDA低显存宣称；不得偷降精度通过。MRT2的风格/续写与双侧重绘不同，不把替代模型冒称等价。

产品面向不同配置的Apple Silicon Mac；本机M4/16GiB只是小规格验收机。SA3small/medium分开登记能力与估算，6秒首测不成为产品时长上限。文本维持4-bit、FLUX维持q8；更多模型、分辨率、上下文需逐配置标明声明/适配/真实验证。MLX仍不代表Intel Mac已支持；界面/后端系统门槛分开。[批次记录](tasks/D-AUDIO-BACKEND-01.md)。

后续顺序：本后端真实生成/取消/变体/重绘验收→创作者可听候选比较/采用与安全保存（按已有UI候选衔接）→依据所选模型确实支持的条件做谱面/哼唱/歌词控制适配，紧接专门歌声里程碑，TTS单列。高阶文本、全部META、完整图像增强都不是音乐前置。

以下为本次后端候选之前的历史装配/路线检查点，不覆盖上方已更新状态。

2026-09-09装配状态：声音项目服务和生产UI已经在隔离批次通过CPU/离屏检查与完整无签名编译，推送保存候选；真实录音/试听/原生面板/旧项目副本迁移待人工清单H09/H10，源默认尚未接纳schema4。此段覆盖下方早期“接线待开始”状态，不改变历史记录。完成真人门槛后，紧接HUM可编辑音符/MUS-0与MIDI，再按下列验证规格接MUS-1受控器乐和MUS-2歌声。媒体导入/播放不是AI声音生成，当前没有音乐模型实现或下载。


## 2026-09-09：音频推理生成的明确后续位置

用户在批准H08时再次要求适时完善音频推理生成。该方向已采纳；本次实施授权仍为HUM原声工作台及其限定收尾/装配，未批准音乐模型、声库、vocoder下载或推理研发。下方9月8日阶段状态保留历史，本节与CURRENT_ACTIONS覆盖旧停点：T0和有限PNG工作台已完成真实验收；HUM原声基础组件及H08已在隔离批次验收：类型/实际格式/滚动问题和重播失败的定时器释放均已关闭，组合CPU通过。共享会话与生产UI接线、真实录音/试听和源默认迁移尚未验收；下一阶段先完成声音工作台使用闭环。录音、解码、波形与试听属于媒体能力，不能计作音频模型生成已实现。

推进出口：原声导入/录音/片段/保存重开可靠后，下一产品提案优先细化HUM音高到可编辑音符与MUS-0八小节/普通试听；明确的音符/和声/演奏输入可用后，接一个MUS-1受控本地器乐生成候选，随后紧接MUS-2旋律与歌词演唱。独立的数据/输入部分可交错，音乐后端可提前做有限设计，不等所有文字、图像或META增强。TTS单列，不用旁白替代歌声。

MUS-1启动前必须提交一份小型、可审阅的模型验证规格：实际支持的输入及精确/近似/不支持边界；用户锁定的作品数据和渲染声音的区别；8小节或更短片段、局部重生成/候选采用/保留原件；模型/权重/声库/vocoder各自许可和固定版本；显式采样率/通道/精度/量化。候选沿用下方索引，尚未选型，宣传支持Mac不算本机通过。

后端接口届时按真实候选能力补到既有请求/运行时边界，不提前创建空库或通用DSP框架：提交时冻结作品与演奏版本、输入引用和实际参数，返回进度、取消/失败、最终音频资产和可追溯配方；不从变化后的界面重读参数，不把原始数组传到视图。播放/录音状态与生成任务分开；生成需要的重计算资源仍统一调度，结束和释放后才交接。

本机M4/16GiB以短片段、单个重任务、离线先行为验证起点。分别测首音、局部控制延迟、吞吐、峰值与重复/取消释放；听感和结构服从需要单列人类检查，成功解码不能替代。新依赖/权重、权限及声音质量承诺按既有规则单独授权。当前目标是保留清楚的生成出口，不在H08期间插入模型实验。


2026-09-08，v7 用户需求已采纳；本文为路线和验收出口，非音乐实现授权或本机模型实测。当前只实施 T0/META 有限批次。详细外部研究是提案，不把模型名、研究性能或许可证推测当作已接入事实。架构取舍未定，不生成冻结 ADR。

## 模态推进与有限出口

固定图像范围的生成、取消/释放、保存重开、比较/采用、安全导出已有历史验收，见 EXPLORATION_STAGE_ACCEPTANCE、IMAGE_RUNTIME_ACCEPTANCE。当前签名构建完整 UI 和跨构建授权恢复缺口仍作为相应交付门槛，不阻塞无依赖的文字/音乐数据与 CPU 工作。参考图、蒙版、更多模型、RAW、LoRA、节点是后续增强，不要求先穷尽图像路线。作品丢失、错误参数、取消未释放和不安全导出必须阻塞受影响交付。

文本已有真实本地推理基础（不是 TTS）；T0 接入草稿、选段改写、候选比较、接受/拒绝/撤销、保存重开。先验编辑/版本/取消流程，不预设当前小模型具备专业音乐规划水平。本批只交付可测试编辑服务和草稿 Data 归档，完整生产保存/界面/真实模型闭环待后续验收。不把高级文本功能完成作为音乐前置。

| 路线 | 结果与范围 | 依赖/出口 | 状态 |
| --- | --- | --- | --- |
| T0 | 一份稿件选段修改，异步旧版本不覆盖，接受/撤销和保存重开 | 复用既有文本核心；组件后接工作台存储/界面与真实验收 | 用户要求；有限组件已本地验收，完整工作台闭环待验收 |
| HUM-1 | 短单声部无明显伴奏哼唱，立即保存原声，录后音高/音符，纠错/试听/导出 | 录音不等模型；先已有文件输入/普通音源，麦克风权限另行处理 | 用户要求；未开工 |
| MUS-0 | 8 小节可编辑旋律/和弦，普通试听，MIDI与明确 MusicXML 子集往返 | 与 HUM 共用音符/时间解释；先冻结输入子集、保留原件，未支持项可见 | 用户要求；待有限设计/实施授权 |
| MUS-1 | 一个受控本地器乐候选、局部修改、导出音频与作品数据 | MUS-0 数据/试听可用；候选控制/资源/许可本机验证 | 方案候选，未选定实现 |
| MUS-2 | 紧接旋律＋歌词的可控演唱，局部时值/音高/发音调整和导出 | 明确语言/合法声库/vocoder；不以 TTS 替代 | 用户要求；明确紧接里程碑 |
| HUM-mobile | 手机离线捕捉，Mac 接续编辑 | 原声/音符/解释可携带且无账户/绝对路径耦合 | 用户要求；远期捕捉伴侣，不建 DAW |
| TTS | 旁白与配音 | 独立任务和验收 | 保留路线，非受控歌声完成标准 |

下一音乐工作包的明确入口：完成 MUS-0/HUM 共用的音符事件、自由时间与节拍解释契约，验证一个短素材从原声引用到人工可改音符、普通试听及 MIDI 导出；不依赖 META PNG、完整高级写作、MRT2 或 DiffSinger。真正识别模型的选择/下载/精度/最低硬件另行批准。先开发纯数据/CPU 的部分，不能将模型尚待评估当作无期限搁置。

## 创作者所有权与音乐数据

三类对象分别版本化：作品（旋律、和声、歌词、结构、锁定范围）；演奏解释（奏法、力度、微时值、发音）；音频资产（实际声音、上下文/尾音与来源）。时间坐标明确区分原始秒/采样、音符事件和节拍；不暗自把自由节奏量化为固定拍。人类原声→模型转录→人工修谱→模型渲染逐段记来源，不改标为纯 AI 创作。

哼唱另分四层：原声/连续音高、音符事件、节拍/记谱解释、人工修订。录音中断和识别失败不能丢已保存原声。量化/移调/拆合/改拍号均是可撤销解释，不覆盖输入；允许合理多解，用户决定最终记谱。首版不承诺多声部识别、专业全排版或实时神经乐器。验收分别测音高/八度、分裂合并、节奏解释、纠错负担、中断保全，不给未经校准的单一正确率。

锁定音符数据可精确；生成声音服从程度必须分别报告精确/近似/不支持。不能静默将乐谱变成提示词。局部渲染的上下文、尾音和交叉淡化需要明确，不能借边界处理改写已冻结音频。MIDI 事件不等于完整谱面；MusicXML 只接入逐项明确的音高、时值、休止、连音、声部和拍号等子集，未支持输入保留原件并解释限制。

## 候选和验证（未本机通过）

- MRT2 small：器乐候选，官方具音符条件及 Apple Silicon 实现。small 230M 不代表完整运行只占权重大小；codec/style encoder/缓存均计入。官方“快于播放”不等于控制低延迟。资料：[实现](https://github.com/magenta/magenta-realtime)、[模型卡](https://huggingface.co/google/magenta-realtime-2)、[说明](https://magenta.withgoogle.com/magenta-realtime-2)。代码/权重许可分别核验。
- DiffSinger：可控歌声候选，旋律/歌词和表现参数；声库与 vocoder 独立许可。[官方](https://github.com/openvpi/DiffSinger)。
- ACE-Step：补充整段生成/创意伴奏，条件有近似性，不替代严格乐谱通路。[条件边界](https://github.com/ace-step/ACE-Step-1.5/blob/main/docs/en/Tutorial.md)。MIDI-SAG 保留伴奏研究，依赖链较长，[官方](https://github.com/fundwotsai2001/MIDI-SAG)。
- Basic Pitch 为录后音符候选，[官方](https://github.com/spotify/basic-pitch)。SwiftF0 为单音基频候选，需要分音/记谱；名称不证明原生 Swift 兼容，[官方](https://github.com/lars76/swift-f0)。NeuralNote 仅工程参考，处理速度不代替端到端延迟。本轮未选择、安装或实测这些依赖。

M4/16 GiB、外盘、小片段、离线先行，单重推理任务。分别测冷启动/首音、控制延迟、吞吐、峰值内存和取消释放；播放/UI 不在模型计算线程等待。现有文字/图像精度不改；音乐模型和量化显式确定。源码/模型/声库/vocoder/输入素材逐项核验，不假定闭源可嵌入、不要求云服务。新依赖、权重、费用、权限和声音质量承诺另行授权。
