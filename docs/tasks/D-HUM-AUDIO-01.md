# D-HUM-AUDIO-01 录音播放与独立视图

spec_revision=1; batch_id=D-HUM-ENTRY-01; contract_revision=HUM1. Worker model gpt-5.6-terra/medium. Worktree /Volumes/CodexProjects/Codex/D-Worktrees/D-HUM-AUDIO-01. Evidence/output root is master run/audio; writable only this worktree plus audio/worker-output and audio/tmp.

Allowed files only:
- Packages/UI/Sources/DWorkbench/Audio/AudioTransport.swift (new)
- Packages/UI/Sources/UI/Views/AudioWorkbenchView.swift (new)
- Packages/UI/Tests/DWorkbenchTests/AudioTransportTests.swift (new)
- Packages/UI/Tests/UITests/AudioWorkbenchViewTests.swift (new)
No changes to ProjectModels/Store/Session/WorkbenchModel/WorkbenchView/D/Info/entitlements/AudioTypes. Your component must compile against preparation base, without depending on storage task's future ProjectDocumentKind.audio or inspector. Use shared AudioTypes directly. App adapter comes in second wave.

Deliver public @MainActor @Observable AudioTransport with stable public surface:
state: AudioTransportState (idle, requestingPermission, recording, recorded, playing, paused, failed), positionFrame:Int64, recordingSeconds:Double, errorMessage:String?, recordedURL:URL? (private setters), recordingDidFinish:(@MainActor @Sendable (URL, String?) -> Void)?; initializer recordingEnabled:Bool=false. APIs preparePlayback(url:URL,format:AudioFormatInfo,range:AudioFrameRange?=nil) throws, play() throws, pause(), seek(toFrame:Int64) throws, stopPlayback(), requestAndStartRecording(to:URL) async throws, finishRecording() throws -> URL?, shutdown(). Factory injection for deterministic tests is allowed inside this file; no generic framework package. Storage inspector provides validated media in production; still reject invalid format/ranges and unsafe URL/nonregular files. Playback frame coordinates always original file frames; explicit range excludes end. Must stop at range end, not run entire source. Selecting new range while paused/playing invalidates old completion; no old callback stops new session. Stop/shutdown are idempotent and dispose own timer/device; don't retain self forever in polling Task.

Use existing macOS AVFoundation AVAudioPlayer/AVAudioRecorder or system equivalents; do not rewrite realtime DSP or create background shell processes. Prefer Swift6-safe main-actor ownership of delegate objects. RecordingEnabled=false rejects without any OS request or file creation. When enabled, only explicit requestAndStartRecording requests mic permission; state/epoch guards cancel or shutdown during await so late consent does not start recording. Do not call this with actual permission in Worker. Destination passed by storage reservation, unique absent path, no overwrite/symlink-follow; preserve produced bytes, never delete on error. Native capture format CAF mono48000Hz linearPCM float32, bounded auto-stop120s. If hardware cannot honor it, fail visibly; no covert quality switch. Close recorder/flush before reporting recordedURL/callback. Auto-stop/explicit stop/device error callback exactly once; error may preserve partial file URL for app recovery. Timer updates current elapsed/position for visible progress only. Recording and playback mutually exclusive, no MLX/GPU. Don't request or edit app entitlements/Info.plist; Lead controls eventual host capability gate.

Standalone public AudioWorkbenchView with Liquid Glass controls and neutral waveform/content, responsive within current minimum860x580 window, keyboard labels and accessibility IDs. It takes AudioDraftDocument?, AudioAssetMetadata?, waveform:[AudioPeak], a bindable AudioTransport and explicit callbacks (through a small AudioWorkbenchActions value you define) for: import, record/start, finish, save note, select clip, add clip range+name, export original, export current range. Publish exact initializer/actions in final response for second-wave adapter; do not implement store/native file panels in this view. Support empty state, recording/error/pending status supplied from app, full/clip selection, frame-range controls displayed in seconds, waveform seeking, play/pause, document note edit/save, clip name/range creation, original/clip export. Original media note and clip time definitions must remain visible, public no model/settings clutter. Controls invoke adapter closures; no automatic permission request or file write when view appears. Draft changes/new document must not emit stale previous note writes. If requiring state-bearing view model, keep concrete narrowly-scoped public type in view file; do not add new framework.

Required tests without real output/mic: recording disabled no permission/file; invalid/empty ranges; prepare errors preserve original; stop/shutdown/late callback cancellation using injected deterministic device or pure transition test seams; single completion; view hosted wide/narrow/wide keeps content/action reachability and does not fire note/save on init; Unicode note/clip callbacks and immutable input. Real audible play/record is Lead human-gated acceptance, not mock claim. Do not run AV capture/play or SwiftPM in Worker; tests created and parse if supported, Lead runs CPU after handoff.

## Shared fixed contract HUM1 / revision 1

Source base a406af9cd8908a77ae2f013d29121164d6fc0745. Execution base is the preparation commit recorded in the dispatch request, not the old source SHA. Shared concrete values are in Packages/UI/Sources/DWorkbench/Audio/AudioTypes.swift; Lead owns that file. No change to DInference, DRuntime, MLX, current model revisions/precision, signing, bundle ID, entitlements, Info.plist, dependencies/locks, scripts, source branch, user scheme or other task files. No network, model downloads, microphone permission request, actual recording/playback, GUI or full application build in Worker.

Product: short original audio import/recording, display waveform, normal playback/pause/seek, non-destructive named frame clips and notes, safe project save/reopen, original export and explicit clip export. No transcription, score, quantization, music/TTS model, mobile, plugin or broad metadata platform. Current PCM limit: actual WAV/CAF LPCM only, mono/stereo, finite sample rate8000...96000Hz, signed integer16/24/32bit or float32, nonempty <=120seconds and <=64MiB. Check both file bytes and decoded frame count; reject unsupported without converting silently. Waveform at most1024 min/max buckets. Range is half-open original frames, 0<=start<end<=frameCount; no byte/character offsets or beat quantization. Original file preserved byte-identical. Clip export is explicit WAV float32 at original rate/channel count, no resampling/normalization, never described as original-byte export. PCM32 integer -> float32 may lose low bits, originals remain intact. No playback/record operation requires MLX/GPU.

AudioDraftDocument.id == ProjectDocument.id, revision monotonic; assetID permanently references one original. Up to64 named clip ranges, unique IDs, selected ID exists or nil. Nonempty name <=256 UTF8 bytes; note <=16KiB UTF8, preserve exact Unicode bytes including combining characters. New original audio creates a new audio document, never overwrites current original or silently replaces image/text. No AI generation job fabricated for imported/human recorded audio; provenance importedFile/microphone, actual SHA256 plus actual audio format. Media metadata/source identity not inferred from UI labels. Audio bytes/project files are owned by app services, not views.

Recording reservations persist BEFORE starting capture. Pending captures are retained for explicit recovery on reopen; no scan outside exact registered paths, no guessing deletion. Normal stop/autostop/device failure reports once, closes/flushed recorder before import/recovery. Close/navigation while recording must stop+save (or remain open on failure), not drop callback or raw file. Cancel during permission wait invalidates late grant, no surprise recording. Original raw/partial files retained on failures; power-loss recovery not promised beyond persisted data and validated files.

CPU tests use synthetic locally generated PCM, no new downloaded fixture/third-party library. No original D or existing projects. Use only task output/tmp/cache and a unique directory per test; no default py_compile. If Python check needed use tokenize.open+compile(...,dont_inherit=True) in memory, do not execute target. Known nested SwiftPM sandbox failure: Worker must NOT run swift test or disable sandbox; Lead executes exact package checks after handoff. Optional direct installed swiftc -frontend -parse with module cache under task tmp only; any unexpected rejection pauses and reports, no invented fallback. This exception is declared before execution. Swift6 Sendable/actor correctness: no unchecked Sendable/nonisolated(unsafe) or broad catches to erase errors.

Only preflight until Lead explicitly sends IMPLEMENT with same spec revision and execution base. Return task/revision/run IDs, physical cwd/root/common Git dir/branch/fullHEAD, allowed path summary, forbidden scopes and test expectations. No writes in preflight. Lead independently verifies requested vs observed model/effort and workspace-write boundaries. Worker never commits (shared Git management is outside write roots), changes global docs/specs or spawns a child/background process. After implementation return changed paths, behavior, tests created/executed/not executed, explicit errors/events/remaining risks and own process state. Initial+max2 targeted repair rounds, one bounded Lead takeover only if applicable; new task number/model does not reset consumed budget. Every new instruction follows Lead review of preceding permission events and protected state. Ambiguous contract or need outside allowlist: pause relevant action and ask Lead before coding. No acceptance policy changes.

## First-wave stop, 2026-09-09 JST

Status: candidate rejected for acceptance; preserve for a specifically bounded follow-up, not another unapproved repair. Initial and repair1 were Terra/medium (265.365s and277.142s); final ordinary repair2 explicitly used Sol/high after a new read-only model/permission precheck and stopped at900s. Two ordinary repairs consumed. Lead performed the one bounded takeover declared in audio/lead-takeover-scope.json: type correction, exact rate/terminal-state checks, finite-width layout and actual text-field/geometry test instrumentation. This is not independent Terra success.

Actual final compilation succeeded; nine transport methods and three UI methods passed, while the fourth UI method (maximum64 clips, narrow/wide scroll reachability) failed two final-row intersection assertions. Original compile/type and test failures remain preserved. Tested object is ec77 plus exact hashes in audio/lead-tests-lead-finish-compile/request.json; final candidate SHA is external audio/candidate.json. No real playback/recording/GUI or production session assembly was executed.

Nonimplementer Sol/high read-only review thread01a081e4-ffae-7791-b78d-622a9d1c6420 completed normally471.205s. Both observed turn contexts match model/effort/read-only/approval-never. Reviewer did not execute tests. Review found: actual file container and signed integer encoding are not fully cross-checked by playback admission; the scroll failure is likely test targeting/coordinate refresh but actual reachability remains unproven; Unicode UI activation and two Lead transport corrections need discriminating coverage. Lead confirmed the missing checks in current code, without claiming an exploit or hardware reproduction. Review snapshot/hash and review-response.md are immutable external evidence. No further fix is made here; ordinary budget and bounded takeover are exhausted.

Minimum proposed follow-up (requires explicit bounded extension): fix actual container/integer admission with exact counterexamples; identify/drive the correct rendered scroll surface while retaining final-row assertions; add the missing discriminating regressions and rerun focused tests, then nonimplementer review. No contract/quality relaxation, permission change, new dependency/model, or transport rewrite. Only after this gate may the shared assembly Worker be dispatched. Dirty-note/navigation/close persistence are separate assembly prerequisites, not retroactive component defects.

All implementation/review processes ended under owned runner handles; no system-wide process guarantee. Four files plus this record are the only changes. Source branch, user scheme, app, signing and historical evidence unchanged. Cost optimality not established; subscription cost and full Lead attribution unknown.
