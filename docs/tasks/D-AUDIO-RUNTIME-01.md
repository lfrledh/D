# D-AUDIO-RUNTIME-01
Task revision1, contract AUDIO1 host bridge, batch D-AUDIO-BACKEND-01. Sol/high due async process ownership and data protection; no recursive dispatch. Source beaaaf82d845e672c6a3b661d928654affc00518. Execution base is full preparation SHA in dispatch. Initial implementation plus2 repairs; shared contract by Lead; Worker owns concrete host/CLI implementation.

## Scope and exact ownership
Implement actual InferenceBackend adapter for the Python SA3 provider, same DRuntime queue and MLXExecutionLease as text/image, plus explicit developer CLI audio entry. No production UI enablement/schema migration. Allowed new files under Backends/MLX/Sources/DMLXBackend/: MLXAudioBackend.swift, AudioBackendConfiguration.swift, AudioModelInventory.swift, AudioProviderProcess.swift; new tests Backends/MLX/Tests/DMLXBackendTests/AudioBackendTests.swift and Tests/DRuntimeTests/AudioRequestTests.swift; modify only Backends/MLX/Sources/DInferenceCLI/CLIOptions.swift and DInferenceCLI.swift. No other paths, specs/docs, Python provider, Vendor, lock/package/project/signing/app/workbench/text/image implementation or .git writes. No worker commits.

Read Sources/DInference/AudioRequest.swift and InferenceBackend.swift, DRuntime lifecycle, MLXExecutionLease.swift, relevant CLI current code. Read provider specification revision2 from readonly sibling D-AUDIO-SA3-01/docs/tasks/D-AUDIO-SA3-01.md, not entire history. Python implementation develops independently; use frozen JSON, do not import other worker changes or couple to incidental implementation.

## Frozen host contract
Public AudioBackendConfiguration explicitly takes pythonExecutable, providerScript, vendorDirectory, modelManifest, artifactDirectory URLs, profile enum smMusic/smSFX/medium with raw values sm-music/sm-sfx/medium, licenseAcknowledged Bool defaultfalse, timeoutSeconds finite positive default600, cancellationGraceSeconds positive default15. No downloads/interpreter detection/global environment/network/keys. All absolute local safe paths, no output within model/vendor/source or viceversa. pythonExecutable/providerScript must regular readable/executable as relevant; symlinked system executable may be resolved only by trusted host config? For this first scope reject symlink path components for all configuration except python executable: resolve its trusted host supplied path and verify regular executable. No run if licenseAcknowledged false. Initialization/estimate CPU must not import MLX, spawn model process or allocate GPU. AudioBackendConfiguration itself can be constructed before permission.
AudioModelInventory validates common request, AUDIO1 profile limits/capabilities/seed<=2^32-2/guidance1...15/steps1...100, source44100stereo and duration match/no silent conversions. Reads only bounded regular manifest and five explicit files with identity/size checks for estimate; actual Python hashes all before MLX. Model request revision must equal registered da6edc54ddba10bfd79a077102ded687f80e882b; require same model dir for execution. Manifest schema/repository/revision/exactrequiredpaths/positive-size/hash rules as provider. Conservative peak =2*largest five file sizes+1GiB+ceil(seconds)*64MiB, checked arithmetic, confidence estimated. No 16GiB product cap; runtime budget admission rejects based on caller budget. Reject malformed/bool numeric/version/unknown request value cases. Trusted manifest still parsed strictly; explain if Foundation JSON parsing loses duplicate keys and use bounded duplicate-aware strategy or report ambiguity before implementation, not hidden weakening.
MLXAudioBackend descriptor id mlx.audio.sa3 / version1 / capability audioGeneration. One backend instance requires release before another execute. Acquire same MLXExecutionLease before child heavy work; keep until child actually exited and release returns. estimate no file writes. execute creates unique per-run container under supplied artifact root, writes request.json outside empty container/output job directory, launches exact configured python -B script with explicit AUDIO1 args using Process arguments (no shell), environment explicitly disables bytecode and fixes TMPDIR/cache inside own container. Don't pass arbitrary user environment auth vars into subprocess; minimum executable/runtime locale paths and own caches, no code/user PYTHONPATH. Backend receives modelReference.directory from request. Freeze all actual input in request, not later UI state. No queue/task fire-and-forget.
Read stdout and stderr concurrently bounded total16MiB stdout/1MiB retainedstderr (drain excess but terminate malformed/excessive stdout), JSONline max2MiB. Valid progress forwards awaited emit; parse terminal event schema/runID/type, only one terminal; zero process exit plus validated result required. Cancel/errors/emit failure request SIGTERM only owned Process; timeout/grace may SIGKILL owned PID, await exit and reader drains before returning. No busy-loop polling/main-thread blocking. Cooperative termination is a request, no instantaneous GPU guarantee. Handle launch failure, cancelled beforelaunch, failedprocess exit, missing/duplicate/wrongrun terminal, malformedstdout, stderr alone nonfailure, child ignoringTERM and emit errors. Own subprocess only, no pkill/system scans. Process Sendable wrapper allowed only if narrowly justified with actual synchronization; no blanket unchecked actor safety escape.
Result references only output.wav inside exact job, regular/nonsymlink, SHA/bytecount/frame/rate/channels/float32 independently rechecked; parse RIFF structure and finite samples or equivalent bounded CPU parser, no trusting child JSON alone. result.json snapshot is additional private record, not final standalone user-media provenance contract. Emit artifact only after completechildexit and successful validation; InferenceResult metadata includes profile/modelRevision/precision/recordPath; preserve valid published WAV even after consumer error/cancel late. release never deletes published files; no scans. No new broad cleanup API required: owned failed run dirs may remain for host diagnostics; report location, do not delete unknown artifacts.

## CLI
Extend --capability text|image|audio; --audio-operation generate|variation|inpaint, --duration-seconds, --audio-source PATH, --audio-source-sha256, --audio-source-frames, --audio-edit-start-frame/end-frame, --audio-strength, --audio-profile sm-music|sm-sfx|medium, --audio-python, --audio-script, --audio-vendor, --audio-manifest, --audio-license-acknowledged true (explicit; help explains caller must already have rights), --timeout-seconds. Existing --seed/--steps/--guidance/--artifacts applyaudio; audio default6s/8steps/guidance1/seed42, budget8192MiB explicitoverride supported. Source properties44100stereo; provided args required editoperations. --revision requiredaudio registeredrevision. File-report path cannot target model/vendor/input. Existing text/image behavior remains. Add --image-profile verified512|scalableKlein4B explicit flag, reject on nonimage, defaultverified512. This image API is supplied independently; until merged Worker can only parse, Lead compiles combination. No audio-default-workbench enablement. CLI full exit/result must reflect actualruntime and controlled errors.

## Frozen acceptance
CPU AudioRequest validation: threeoperations, invalidopfields, frameintervals, badURI/hash/nonfinite/Unicode/largeUInt64/noBoolean tricks in JSON path. Inventory profile sizes/duration/overflow/manifestcorruption/seed bounds/badsource independently. Controlled Process CPU fixture (owned Python script, no MLX) tests validartifact, stale/wrong/duplicateterminal, nonzeroexit, malformedstdout, failduringemit, cancel thennextjob/lease handoff, timeoutTERMignored, protectedsource/modelfiles/artifactsurvivesrelease, invalidWAV/hash/metadata. Fakeprovider markedfixture notrealmodel. Use XCTest tests in DMLX target; Worker can run swiftc -frontend -parse with owned module cache, no SwiftPM/Xcode/MLX/GPU. Lead runs CPU/build integration after you pause. New DRuntime tests no backend linkage/realmodel. Every subprocess test full exit timeouts, no persistentprocesses.
Read uncertainty -> ask Lead before guessing. Permissiondenial unknown -> pause report, do not change permissions/caches beyond declaredpaths. No network/dependency/modeldownload/keychain/GUI/source writes. Own tree+worker-output/tmp only. Return changed files, checks, blockedparts, error events and owned processstates; no spec edits. Lead will review and test actual Swift6 compilation and realmodelwhenlegal/resourcegates clear.

## Revision2 - Lead correction before IMPLEMENT
The phrase five required weights was a Lead counting error: exactly FOUR files perprofile, one DiT+T5Gemma+encoder+decoder. Use four everywhere, including estimate largest of four. Exact names and mathematical stages unchanged. No fifth invented file. Provider spec has same typo; Lead will append its correction once its active implementation hands ownership back; this explicit host revision is authoritative now. No model rights/precision/acceptance weakening. Foundation duplicate keys: a bounded lexical duplicate-key validator before JSONSerialization/JSONDecoder is allowed inside AudioModelInventory.swift; use actual parser grammar, not substring regex; maximumdepth32/1MiB manifest, tests escaped-key duplicates. Workbench UI stays untouched. Image profile API arrives from SCALE at combination; you may inspect readonly D-SCALE-01 ImageExecutionProfile.swift for exact names, not modify/use that tree in tests. New execution full SHA in dispatch.

## Revision3 - targeted completion after bounded initial run
Initial implementation900s timedout, owned CLI returned-15 with no unmatched started tool/file operations. Four new production files retained in local candidate (not accepted); tests and CLI missing. Lead SDK-qualified Swift6 typecheck found AudioProviderProtocol.readStdout uses private AudioOwnedProcessControl in non-fileprivate signature. Fix actual access control, not unsafe Sendable bypass. This targeted completion counts repair1, one ordinary repair remains; no new task number/budget reset. Lead read initial APIs and environment: remove child HOME override and unnecessary home folder; explicitly supply own TMPDIR/XDG_CACHE_HOME/PYTHONPYCACHEPREFIX/PYTHONNOUSERSITE, no user HOME/CODEX_HOME changes or credential env propagation. New direct typecheck command is permitted under existing CPU policy (below); no SwiftPM/Xcode/model. Exact approved batch snapshotcf2c1f1536939e1d847247e3cfa192b7911489f6 merged before repair, so SCALE ImageExecutionProfile now exists locally. This is authorized baseline synchronization, not unknown drift. Authoritative AUDIO provider revision3 clarifies fourweights and shipped Vendor root. Also review Lead AudioRequest.swift validation/value contract as nonimplementer, return any finding; do not edit shared file, add allowed direct tests. All other ownership/tests/precision/paths frozen.

## Revision4 - bounded final ordinary repair and shared frame clarification
Lead build-for-testing of edb3425e63d925be4b1405ecdf39c4683cb10959 failed AudioBackendTests.swift:304:106, self.manifestText captured by closure before init (weightSize reference implicitly captures self). Production typecheck passed but test parse was insufficient; fix initialization, no weakened assertions. Evidence runtime-lead-test1b/build.stdout.log and build-result.json. Repair1 ended normally with all tool operations paired, two local patch-format rejections were not permission incidents. This is repair2, final ordinary Worker repair; budget not reset.

The Python and Swift rounding ambiguity is clarified prospectively: generation frames use nearest ties-to-even; variation/inpaint use validated source.frameCount after existing <=half-sample duration tolerance, preserving original length. Apply admission and actual validation consistently; test half-frame boundaries and edit source mismatch, no change to model duration/precision.

Lead review found current 'valid' fixture metadata={} is accepted because host compares result.json only with terminal, not with the frozen request. Tighten shared AUDIO1 boundary explicitly: before artifact emission require metadata request snapshot (same actual request except source absolute path omitted), profile, modelRepository/modelRevision, all four weight paths/sizes/digests, vendorRevision, fixed precision dit/text float16 and codec/master float32; no trusting two mutually-consistent wrong records. Compare numeric values semantically without bool coercion or UInt64 truncation, not textual 1 vs1.0. Unknown optional timing/allocation keys may remain; allocation unavailable is null, not fake0. Existing source conversion and inpaint requested/effective regions should be validated when present/required per provider spec. Fixture must populate frozen metadata, then independently corrupt request prompt/seed/source digest, profile/revision/precision/weight hash in BOTH terminal and result.json; each must fail. These explicit field details close Lead host-spec omission rather than retroactive Worker failure. readStderr actual IO failure must make run fail after draining, not merely append a diagnostic and return success. Truncation of successfully drained retained stderr remains allowed.

Audio --cancel-after-steps cannot accurately refer to denoising when common progress includes phase-boundary counts. Reject it explicitly for audio (retain image behavior); audio uses timeout/cancellation through existing runtime API. Do not silently cancel at validation count. This is a clarified CLI non-goal, not removal of product cancellation. All eight allowed files unchanged; no shared AudioRequest/spec/other paths edits. Lead will run full CPU suites; Worker may typecheck test files against existing built modules using its own module cache, but no Xcode build/GPU/MLX/import/model/network.
