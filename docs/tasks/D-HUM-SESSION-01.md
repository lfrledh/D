# D-HUM-SESSION-01 项目与声音生命周期接线

2026-09-09. task_id=D-HUM-SESSION-01, batch_id=D-HUM-ENTRY-01, spec_revision=1, contract_revision=HUM-ASSEMBLY1. User approved next sound workbench stage through acceptance and push. Source baseline55e31bd0827ca63ea23cd5b3de39bd9535637c9c; accepted isolated foundatione902c9beff430c54ae48cc6795de1fbf8eaa5208 (tested211ae96ec508e086d6ff901936cc524f02a20aa3, then docs only). Actual execution base is preparation commit in request. New assembly work, not renumbering H08 repairs. Lead owns specification/shared integration; this is the single service implementation writer. UI follows accepted service API, no simultaneous shared-file edits.

## Ownership and scope

Allowed ONLY Packages/UI/Sources/DWorkbench/State/ProjectSession.swift; new Packages/UI/Sources/DWorkbench/Audio/ProjectAudioController.swift; Packages/UI/Sources/DWorkbench/Project/ProjectStore.swift for a narrow read-only registered audio inspection API; new Packages/UI/Tests/DWorkbenchTests/ProjectAudioSessionTests.swift. No other edits without Lead revision. Do not change AudioTransport, AudioTypes, existing tests/assertions, schema/migration policy or other audio STORE code. No UI, bootstrap, signing/entitlements/Info.plist, project/lock/dependencies, scripts, global docs, source branch, shared Git metadata or other task. Contract signatures below describe required operations; exact local helper design remains yours. If a required operation cannot fit, pause and report before expanding scope.

Read AGENTS.md, this task, relevant ProjectSession/ProjectStore/AudioTransport/AudioTypes and existing ProjectTextController/ProjectSessionConcurrencyTests/AudioProjectStoreTests patterns. Do not load all task histories, models or ADRs. CPU fixtures only. No network, models, AV hardware/permission, visible GUI, application build, recursive agents or commits. Sol/high requested; Lead verifies runtime independently.

## Frozen behavior and public surface

Use concrete @MainActor observable ProjectAudioController owned by ProjectSession for each open project context, with transport, active persisted AudioDraftDocument/metadata, waveform, busy/error/dirty state. Constructor injection of AudioTransport is allowed for @testable deterministic lifecycle tests using existing internal device factory. ProjectSession init gains audioEnabled:Bool=false and audioRecordingEnabled:Bool=false (existing callers compatible); recording capability false fails clearly before permission/reservation. Controller exists only for an enabled open project. UI will explicitly choose enabled isolated host later. This gate is NOT a claim of preventing the existing batch schema4 migration; source default stays unchanged until product gates.

Provide public operations via ProjectSession/controller sufficient for: importAudio(at:name:) async Bool; startAudioRecording(name:) async Bool; finishAudioRecording() async Bool; retryPendingAudioCapture(id:) async Bool; save note/add clip/select full or saved clip with async Bool acknowledgement; original/clip export to explicitly selected destination with captured context/document; active audio inspection refresh. Expose a per-open contextID that changes even on close/reopen same ID/path. Readonly metadata/inspection runs on Store actor with registered hash/format verification; no raw media data/NSImage/window/bookmarks in service API. Frame ranges remain original half-open frames, original bytes never overwritten. No automatic playback on load/navigation.

Controller should own or explicitly track unsaved note/clip-name/range inputs so they survive failed saves/navigation. Choose one clear ownership strategy and report its UI binding API; API must allow marking unsaved input with context+document identity and only clear acknowledged matching snapshot. No silent discard. Saving note or adding clip must snapshot input+expected document/context, serialize each store write, assign revision exactly current persisted+1, retain newer edits made during await, and returnfalse/error without changing saved draft on failure. Existing duplicate/Unicode/clip limits unchanged. Save selection as draft through same serial lane, prepare exact selected range after success without autoplay; invalid selection does not mutate persisted draft. Independent operation failure never clears unrelated editor input.

## Critical lifecycle table

| Trigger | Required behavior and counterexample |
| --- | --- |
| Import while current draft dirty/candidate active | Require existing text navigation guard and audio dirty guard/flush; hold source security scope until owned copy; capture context/store, publish only same context. New original creates its own audio document. Missing/truncated input preserves source, current document and errors. |
| Navigation/close/relocation | Block unsubmitted audio editor input with actionable message (explicit discard permitted through separate public action only). Complete already admitted save/import writes before changing active document/store. Stop playback before switching. Stop/cancel/finalize recording BEFORE generic isBusy drain so close never waits on itself. Text/image guards continue. |
| Request permission and cancel/close | Persist reservation before permission. Invalidate pending capture generation; late grant never starts. Empty/failed capture reservation remains recoverable/visible; explicit cancelled permission can release active in-memory busy state without deleting any file. Do not treat pending permission as success or endlessly await a system dialog after cancellation. |
| Recording stop/autostop/device error | Callback once; always close device/flush before attempting finalize. Schedule a single tracked finalize task/tail; close awaits it. Successful finalization publishes only matching context and creates new audio document. Validation/save failure retains reservation+raw bytes; actionable error, block unsafe current close or provide explicit keep-for-recovery choice without deleting. Retry same registered reservation, not a scan. |
| Snapshot mismatch or stale async | Context UUID+store identity+document/version gates; reopen same path still new context; no late result replacing unrelated UI/document. Do not infer identity from projectID alone. |
| Existing image/text/PNG | Image gallery remains image/png only, including showingAllArtworks. flushDraft must not pass .audio to image saveDraft. Generation/model/fork/recipe/adoption guards reject audio explicitly where relevant. Generic actual audio export still retains secure no-overwrite semantics. |
| Save failure/close failure | Keep usable controller/store/leases/input; no shutdown before data drained, no swallowed success, no automatic remove/rollback of user files. Subsequent retry possible. |

## Mandatory tests

Use new ProjectAudioSessionTests with unique task temp/settings, fake WorkbenchSession engine as existing tests, synthetic WAV/CAF bytes, injectable transport devices only. Prove session import→note/clip/select→save/close/reopen→original and clip export (original bytes equal); max/invalid range rejection; note Unicode including combining sequence and emoji byte equality; rapid serialized draft changes do not skip revision; stale context/document rejected; unsaved input blocks switch/close and is retained; actual save failure preserves original/persisted metadata and dirty input; image/text creation/navigation remains valid and audio excluded from image gallery; disabled recording never requests permission/reserves; cancellation while permission pending never starts on late grant; stop/autostop finalizes once; device/finalize failure keeps raw/reservation and does not deadlock close; pending recovery explicitly retries. No weakened original assertions or mock claimed as real recording. Avoid artificial 100% assertions that merely mirror implementation; test externally visible state/files.

## Execution and handoff

Physical write root /Volumes/CodexProjects/Codex/D-Worktrees/D-HUM-SESSION-01 plus this run service/worker-output and service/tmp. Full run root is in preflight prompt. Networkdisabled, approvalnever, workspace-write; shared Git dir and source/worktrees other than your own not writable. TMPDIR/Python/module cache within service/tmp. Known SwiftPM nested sandbox issue: do not run swift test or disable sandbox; Lead compiles/tests after handoff. Optional installed swiftc -frontend -parse with module cache inside service/tmp. Python checking only tokenize.open+compile(...,dont_inherit=True), no executing target/default py_compile. Unexpected refusal pause/report; only predeclared cache path is permitted, no invented workarounds. Max900s each delivery; initial+2 targeted repairs, then one bounded Lead takeover if needed, no budget reset. Before every further instruction Lead checks prior permission/events/protection.

First PRECHECK ONLY: return task/revision/run, realcwd/Gitroot/common/HEAD/branch, allowed paths/bounds and test summary; no implementation until Lead sends IMPLEMENT on the same verified chain. Do not claim model identity from self-description. IMPLEMENT handoff includes changed files, exact public surface for next UI worker, tests created/executed/not, anomalies, evidence, own process state. Worker writes only own report/output, not this task file and not Git commits. Lead reviews actual hashes and produces local candidate. Real hardware/GUI and minimal microphone declarations remain separate, unexecuted gates; user wants unresolved manual items in existing queue, not per-fix approval requests.

## Initial delivery and repair1 disposition

Initial Sol/high process hit the declared900s boundary and ended withSIGTERM; no unpaired command/file event, no observed permission refusal or successful out-of-scope write. Two readonly searches used a wrong ProjectTextController path; no effects. Candidatec36cd9968e7b932fda76c12f118cc62661464a60 retains three implementation files; required new session tests were not delivered. Lead full CPU compile/regression reports207 existing tests,206 passed/1 optional installed-weight skip; no new service behavior acceptance. No Lead product rewrite.

Repair1 uses the same four-file ownership/spec1/HUM-ASSEMBLY1, not a new task or budget. Counterexamples clarify existing contract: canonical-equivalent different Unicode bytes cannot be skipped as unchanged; all-document navigation must drain recording before generic busy/flush; cancelled permission must not leave a later close spinning; relocation must replace old controller/store context; autostop must clear finalized busy ownership without requiring a later close; keep-for-recovery cannot detach a live recorder; close admission must reject/preserve late editor changes; successful refresh/import/reopen must prepare exact playback without autoplay; export current explicit frame range must be available for existing view action. Implement the missing required tests, then report exact public API and anomalies. After this repair, one ordinary repair remains plus the existing bounded Lead escalation rule.

## Repair1 verification and final ordinary repair

Repair1 again reached the declared900s process boundary (SIGTERM), with all command/file events paired and no nonzero command or observed permission refusal. Lead committed the handed-back four-path candidateff277437eb2e2a7214431761d413c7e5113b1daa. New session suite compiled:11 methods,10 passed/1 failed (three assertions in autostopSettlesOnceAndFinalizeFailureRetainsRegisteredRawForExplicitRetry, lines406-408). This is Lead actual CPU verification, not Worker self-report. Explicit retry is rejected by the same capture-failure navigation gate it must recover. Evidence service/lead-tests-repair1 under run-20260909T055825Z-assembly.

Repair2 is the last ordinary repair, spec1/HUM-ASSEMBLY1 unchanged, same four allowed files. Preserve all existing assertions. Target: admit explicit registered failed-capture recovery without waiving dirty input/active recording/text work/closing safety; add regression for cancelling a second permission request after an earlier successful recording, where AudioTransport restores recorded rather than idle; make byte-sensitive dirty observation observable (a standalone real Observation probe found canonical-equivalent String assignment changed bytes without notification). These are counterexamples to existing recovery/cancellation/Unicode observable-state requirements, not new product scope. Lead has made no product rewrite. Prior runtime contexts matched Sol/high,approvalnever,networkfalse,task-only workspace roots; source HEAD/scheme still match protected snapshot. Runtime hidden service resolution remains unknown.

## One bounded Lead takeover: capture/editor admission

Sol repair2c07f894b50efceabf7c6d81654ad1bc637ff5754 passed12 session CPU methods, but Lead review found a new counterexample to the frozen no-discard rule: edits during an active recording were accepted into the old document, then autostop published a new document and discarded them. A new13th test first failed on the unmodified Sol implementation with4 assertions (service/lead-tests-lead-counterexample). No prior assertion was removed. Lead used the authorized single bounded takeover: expose observable canEdit, reject editor/selection submissions during capture preparation/permission/recording/finalization, and keep newer edits allowed during ordinary saves. Seven local admission guards changed; no media/store/transport or schema changes. New input is refused before admission, persisted original and raw capture stay safe. UI must disable corresponding editor controls using canEdit while keeping finish/cancel reachable. This is Astra Lead code, not Sol independent success; non-implementer review required before service acceptance. Ordinary repairs exhausted; this takeover failure cannot trigger another loop.

## Review finding countercheck (no second product fix)

Full Lead CPU at a806cbe77324e279eba6e59cafaa7184e1c27354:220 methods/28 suites,219pass plus1 optional installed-weight skip, no failure;13 session methods included. Sol readonly review accepted the capture/edit delta but reported successful recovery leaving admissions closed. Lead traced the actual publish callback: ProjectSession.installAudioController lines402-404 calls loadActiveDocument on the new document, which resumes admissions at line217; Store finalize always creates a new document UUID. The existing recovery test was extended without changing production: canEdit, actual note edit/save, close/reopen all pass (service/lead-tests-reviewer-countercheck). Awaiting review disposition of this evidence; no assertion weakened and no second implementation takeover.

Review tooling event: PATH git invoked xcrun and attempted denied /tmp cache writes while returning0; reviewer continued one read command. Lead prompt omitted mandatory installed Git path. Lead stopped owned pid10421, all tool events paired/worktree unchanged, and resumed once using exact installed Git in unchanged readonly permissions. No successful out-of-root write observed; hidden side effects unknown. The resumed command completed0 without a new refusal. This environment/instruction issue is separate from product quality and does not need broader Mac permissions. Evidence service-review/git-shim-event.json and both review histories retained.
