# D-HUM-UI-01 Sound workbench assembly

2026-09-09. task_id=D-HUM-UI-01; batch_id=D-HUM-ENTRY-01; spec_revision=1; contract_revision=HUM-UI1. User authorized this sound assembly stage through validation and push; hardware/host capability/manual gates remain H09/H10. Lead owns shared contracts/integration. Sole UI implementation writer follows accepted service; no parallel edits to ProjectSession/Store/AudioTransport. Actual source, service and execution SHAs are in preparation request and appended checkpoint.

## Exact ownership

Allowed: Packages/UI/Sources/UI/State/WorkbenchModel.swift; Packages/UI/Sources/UI/Views/WorkbenchView.swift; Packages/UI/Sources/UI/Views/AudioWorkbenchView.swift; one new UI-only helper Packages/UI/Sources/UI/State/AudioWorkbenchPresentation.swift if useful; one new Packages/UI/Tests/UITests/AudioWorkbenchAssemblyTests.swift; D/WorkbenchBootstrap.swift only debug-isolated audio configuration injection. Existing AudioWorkbenchViewTests assertions stay unchanged. No DWorkbench service, project format/store, AudioTransport, assets/models/dependencies/Package.swift/locks, signing/Info/entitlements/project setting, user scheme, scripts/global docs/other tests edits. Need outside scope: pause and report, do not guess.

All Git reads MUST use /Applications/Xcode.app/Contents/Developer/usr/bin/git, not PATH git or /usr/bin/git (xcrun cache-writing shim). Do not invoke xcrun. Read task + AGENTS + accepted ProjectAudioController/ProjectSession audio public methods, existing WorkbenchModel/View, AudioWorkbenchViewTests, WorkbenchFacadeTests, Bootstrap test-session guard. No full history. Request Sol/high,task-only workspace-write,networkfalse; max900s/run, initial+2 targeted repairs then bounded Lead escalation if necessary. Worker does not commit, edit task record, run SwiftPM/full build/GUI/audio hardware/MLX/download/permissions/children. Lead runs CPU/hidden-host and app compile; optional installed swiftc parse cache only in declared task tmp.

## Product and identity contract

When an explicitly audio-enabled isolated project is open: sidebar offers import existing WAV/CAF PCM and recording status/action, audio documents use waveform icon. Selecting audio shows real AudioWorkbenchView fed by its registered inspection/controller, no image canvas/PNG actions/generation inspector/recipe action mislabelled for audio. Text/image paths remain behavior-compatible. No automatic play/record on opening. Editor input/selection controls must use controller.canEdit; recording preparation/finalization may not accept edits into the old document, while finish/cancel stays enabled. Recording capability defaults false; visible plain explanation that microphone host setup is pending, disabled start before any permission request/reservation. Finish/cancel remains reachable for injected recording/pending permission; finalize/busy/error/pending registered recovery shown with explicit retry/keep-for-recovery choices. No repair files or delete original actions.

WorkbenchModel initializer passes optional audioEnabled=false and audioRecordingEnabled=false to ProjectSession, existing callers unchanged. Bootstrap DEBUG can enable audio only with BOTH a valid existing D_UI_TEST_SESSION UUID and D_AUDIO_WORKBENCH_TEST=1. Recording always false in this stage. Missing/invalid test token cannot enable it. Release/default behavior not enabled. This flag does not gate candidate Store schema4 migration: entire batch stays isolated/pending real migration acceptance; do not claim an ordinary candidate build is safe for user projects. No new host permission declarations.

Native file panels belong to UI and use typed argument passing, explicit one source/destination, no scan/auto-read references. New minimal injectable native-panel seam permitted in UI to test suspended panels without showing windows. Capture controller contextID+documentID+range/export format before awaiting; reject after project close/reopen, document/range intent change, stale controller or choosing-location conflict. Cancel panel writes nothing/sets no success. Original export preserves registered WAV/CAF format and bytes; range export is exactly float32 WAV at original sample rate/channels, not a byte-for-byte copy of the source container. Save panel extension/content type must match and explain the conversion. Session handles source security scope and atomic no-overwrite export; UI must not duplicate copy/persistence or fabricate success. Import registration and recording should not clear image comparison/state before a failed action.

## Editing/async behavior table

| Event | Required result |
|---|---|
| Note/clip name input | Production view binds through controller context+document setters immediately; byte-distinct Unicode edits observed. No isolated view-only dirty state lost on nav. Existing stateless preview/test initializer may be retained; production never uses its untracked fallback. |
| Range edit | Use finite frame-based slider/stepper controls (one-frame increments, display seconds), avoiding unparsed numeric text in this first assembly. Clamp to original frame bounds, reject start>=end at action admission without changing saved selection. A user range edit marks controller clipRangeInput pending; initial display of full/selected range does not mark dirty. No fake sentinel string/range. |
| Save note/add clip | Capture actual context/document/inputs, await Bool acknowledgement. Service owns serialization/revision and clears only matching acknowledged clip snapshot. Never clear form/announce saved merely after enqueue. Subsequent edits while save awaits remain. Failed write retains exact entered note/name/range and original. |
| Unsubmitted input/nav/close | Existing controller gate blocks; show meaningful save/discard message. Explicit discard calls controller discard and resets UI only after matching context acceptance. Model file-panel/close guard also applies. Do not add a second draft persistence store. |
| Selection/play/export | Full/saved clip selection persists through service; exact range playback prepared without autoplay. User play is explicit. Keep ordinary pause/resume and waveform seeking on the currently prepared range; do not prepare from its start on every resume. A separate explicit audition-current-range action may prepare the unsaved finite range then start. Track transient prepared intent in UI if necessary, never label one range while playing another. Exports capture immutable range/doc/context and go through Session; unsupported/stale requests fail visibly. No overwrite. |
| Refresh/save/re-render | Do not reset editor on every manifest revision; preserve same-controller input during await and resize. Reopening same project creates a new context; stale closures cannot mutate it. |

Preserve Liquid Glass native button style and prior responsive AnyLayout/editor identity. Controls remain reachable at existing narrow520/wide1000 sizes and max64 clips by scrolling, not horizontal clipping. App's minimum860x580 remains. UI text uses plain Chinese; no schema/runtime jargon except actionable errors. No audio AI generation, model/music controls or unavailable buttons pretending capability.

## Frozen acceptance

Existing UI package suite incl audio view layout/Unicode guards, image/text operations and service lifecycle continues unchanged. New tests must exercise production facade/view bindings with synthetic WAV and fake transport/panels, no AV hardware or shown UI: import->inspect/waveform->edit note+clip->select/prepare->save/reopen->export original/exact range, bytes preserved; original untouched until valid action; failed/duplicate exports/input malformed do not alter existing files; panel cancel and suspended-panel context/doc/range invalidation fail safely; unsaved inputs block nav/close and explicit discard works; acknowledgement/newer Unicode edits; disabled recording never opens hardware; audio routing/hidden PNG+inspector; debug configuration only valid isolated flag/token. Use offscreen NSHostingView for real layout/input components (no GUI automation claim). Keep old assertions; don't lower criteria. Actual device recording/playback, native permission/bookmark behavior and visible GUI remain H09/H10, not mock-pass claims.

## Execution and handoff

Worker runs first PRECHECK ONLY, verifies requested revision/real cwd/gitroot/common/head/branch/write roots, waits for IMPLEMENT on same verified chain. Write roots: own physical task worktree + run/ui/worker-output + run/ui/tmp, network disabled, never approval, no shared Git writes. All outputs/cache/temp declared under own run/tmp. Unexpected refusal pauses; no global cache/permissions. Return changed paths, API/binding decisions, tests added/run/not run, errors/anomalies, own process state. Lead is separate non-implementer reviewer and actual tester; no other independent-review-model claim unless actually performed.

## Preparation snapshot

Source55e31bd0827ca63ea23cd5b3de39bd9535637c9c (already pushed), batch/service combination9f2bb4eb176d04363dae6884d9b461506c54c6be. Full220 methods/28 suites,219pass+1 optional weight skip, no failures in combination/lead-tests-service-integrated. Service737d3dfbb4fd7956e084e01dcf77ff351259fbf1 includes accepted Lead capture-admission fix; reviewer final acceptance in service-review/finding-check-response.md, prior false finding and Git shim event retained. UI actual execution baseline is this preparation commit, recorded in external ui/preparation.json and precheck prompt. No UI implementation yet.

## Initial delivery and repair1

Initial Sol/high timed out at900.024s; all command/file events completed, no active writer left. One rg read included nonexistent Packages/UI/Sources/DInference (exit2); no observed permission denial/expansion. Lead initially treated all nonzero commands as an audit assertion, then classified this read miss before proceeding; no project change in the failed helper. Four-file candidate retained; WorkbenchView routing and required new assembly tests still absent. Actual CPU compile (0253 base plus recorded exact dirty hashes, then identical candidate commit) failed on ambiguous select(nil) and UTType initializer function reference; no behavioral pass.

Repair1 remains spec1/HUM-UI1 and same six allowed files, no service changes. Complete routing and required production tests; fix actual compiler errors. Existing contract counterexamples: import panel must reject same-project active-document changes; opening/reselecting audio must trigger explicit registered inspection without autoplay and correctly initialize range when metadata arrives after view appearance; saved-clip selection/async save acknowledgement must not overwrite newer editable range, note/name or apply stale UI status/preview to another context; stale production action closures need origin identity before scheduling async work, not only capture whichever document is active when a delayed Task begins. Use canEdit for editor/selection while finish stays reachable; pending recovery UI must be available even when active document is image/text. Test production initializer and facade, not just legacy preview/actions. Source/model/signature/permission untouched. After this repair one ordinary repair remains.

## Repair1 result and final ordinary repair

Sol/high repair1 reached900.026s with every started tool event paired/completed. Two malformed readonly glob searches, a no-match rg and a swiftc parse failure followed by local correction were observed; no new permission refusal/expansion. Candidate5abf2c3186bc2a32c7ecbd65b8142fedf0808a9b adds five scoped paths including real assembly tests. Lead actual CPU compile failed before behavior: AudioWorkbenchView.statusText switch needs explicit return. Previous two compiler errors are resolved. Source scheme/hash/index and ordinary app remain protected.

Final ordinary repair2 keeps spec1 and six-file scope. Fix actual compiler diagnostics and complete frozen behavior tests. Lead static counterexample: view appears while ProjectSession.isChangingProject=true, refresh returnsfalse at Session guard; neither document nor metadata changes on gate release, so onAppear/doc-only refresh never runs again. Add a lifecycle-gated refresh tied to the actual opening/navigation transition; no polling loop, no service changes, no autoplay. Test registered metadata initialization through the production view/opening gate, without manually calling refresh to mask the missing UI trigger. Failed refresh needs visible retry and truthful status, not a prepared label before playback preparation succeeds. Keep editable range separate from prepared range and preserve dirty inputs/ack revisions. All async test synchronization must be bounded; hidden-window tests are not real GUI. Clarifications do not revise acceptance or old assertions. After this run zero ordinary repairs remain; at most one bounded Lead takeover, separately attributed/reviewed.
