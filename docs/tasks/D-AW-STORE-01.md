# D-AW-STORE-01 / AW1-S1

Task for generation persistence, prepared by Astra under D-AUDIO-WORKBENCH-01. Actual base is the integration SHA AFTER the separate RECORD handback, never a guessed SHA. Sol/high because schema compatibility, file safety and authoritative job outcomes are coupled. Initial+2 targeted repairs, then one bounded Lead takeover; 900 sec/execution. No recursion. This task must not overlap a recording writer on ProjectStore/AudioMediaInspector.

Allowed: Project/ProjectModels.swift, Project/ProjectStore.swift; Audio/AudioCreationDraft.swift only add request-building/validation helpers without changing existing public fields; Audio/AudioTypes.swift only AudioOrigin.modelGenerated and explicit generated-media bounds; Audio/AudioTransport.swift ONLY optional bounded playback limit parameter/default preserving old recording and import limits; Tests/AudioTransportTests.swift only matching playback bound tests; Audio/AudioMediaInspector.swift only parameterize bounded generated-media inspection, preserve original and RECORD descriptor semantics; new Tests/DWorkbenchTests/AudioCreationStoreTests.swift plus minimal migration expectations directly affected by schema5. No ProjectSession, WorkbenchModel, views, recording creation/writer logic, DInference, backend, signatures/permissions/config/dependencies/docs. Existing public tests' assertions not weakened.

Frozen storage/API:
- ProjectManifest schema5, explicit versionFourBackupFilename project.v4.backup.json, migrate v1/v2/v3/v4 keeping original manifest bytes and media. Existing audio original docs remain audioDraft; add ProjectDocument.audioCreation: AudioCreationDraft? default nil, Codable optional. Exactly one audioDraft or audioCreation for kind.audio. Existing init call sites default unaffected. Image/text forbid audioCreation. Never enable ordinary app migration as part of this task.
- func createAudioCreation(name: String = "声音创作", sourceAssetID: UUID? = nil) throws -> ProjectManifest. New kind.audio/audioCreation, no audioDraft. Optional source may be any same-project valid audio original/result; no PNG/cross-project. default generate/no source, variation/with source; immutable source relation for this document. No new copied asset or automatic acceptance.
- func saveAudioCreation(_ draft: AudioCreationDraft, documentID: UUID, expectedRevision: UUID) throws -> ProjectManifest. Preserve incomplete strings, Unicode and full integer text. Caller supplies new revision; mismatch rejects without overwrite. rejectedAssetIDs cannot be changed via draft-save: compare with existing state; decisions use separate API below.
- func prepareAudioCreationSource(assetID: UUID, runID: UUID) throws -> AudioSourceReference. Check registered digest and actual compatible 44100Hz/stereo PCM WAV. Create an exclusive exact byte COPY via rootFD under AudioInputs/<runID>/source.wav, outside Tasks (backend forbids its artifact root overlapping reference). Frozen source bytes are authoritative request input; recheck source identity/digest, source not modified. No resampling/normalizing/conversion. Collision rejects; failure preserves any owned pending copy, never deletes unknown files. Original sources and generated results both allowed. This is a task input snapshot, not a falsely declared original or a new model output. Request record retains exact source URL/digest/frame metadata and doc.sourceAssetID records lineage.
- AudioCreationDraft.makeRequest(source: AudioSourceReference?) throws -> AudioRequest. Parse finite numeric text; UInt64 seed exact. DInference common validate and current supported profile shared shape apply (seed<=UInt32.max-1, steps1...100, guidance1...15); concrete duration/model budget remains backend. generate: no source/editRegion, actual strength1; variation: source, no editRegion, actual duration source.frameCount/sampleRate; inpaint: source+explicit valid halfopen region, actual duration source. No silent clamping/default numeric values. Requested values are fixed before submit.
- Extend existing enqueue(request:documentID:) to accept .audio only for audioCreation doc, consistent operation/source; validate run input's exact AudioInputs/<requestID>/source.wav path and SHA against doc.sourceAssetID metadata, and actual readable file, before job commit. image behavior unchanged. Requests durable before runtime admission.
- Extend complete(id:result:) for audio jobs: exactly one artifact audio/wav in Tasks/<lowercase run UUID>-<UUID>/job/output.wav, no arbitrary descendant. Full decode/finite PCM/header/44100stereo float32/frame count versus fixed request; bounded resources. Metadata AudioOrigin.modelGenerated, role.result/jobID exact. Only authoritative runtime completed marks completed; no implicit adopted/selected changes. Save failure keeps bytes. UUID asset identity stable once committed; duplicate deliveries reject or return existing only under precise idempotency checks.
- Existing setSelectedAsset/adoptAsset usable for audioCreation with same-document completed/nonrejected candidate or source for selection; adopt nil returns original/no candidate. Add setAudioCandidateRejected(id: UUID, rejected: Bool, documentID: UUID) -> ProjectManifest: decision belongs same doc; reject never deletes, clears adopted/selected if same asset; does not choose another candidate; un-reject restores availability only. Changes update audioCreation.revision; enforce all relationships at manifest validation. Recovered but interrupted result cannot be adopted as a successfully completed candidate.
- func inspectAudioAsset(id: UUID) throws -> AudioInspection and func exportAudioAsset(id: UUID, to: URL) throws: validate registered media/hash; preserve exact bytes and existing target-scope/no-overwrite/export-checkpoint conventions. Audio originals continue old export APIs unmodified.
- recover() must recognize registered audio task's exact published output path; validate output and recover reference only as interrupted when authoritative completion missing, never auto-completed/auto-adopted. Missing/incomplete/corrupt files visible reason, preserve bytes. Cancelled/failed task with any provider output must not magically become successful; no scanning arbitrary audio dirs.
- Playback: preserve public preparePlayback default120sec/64MiB. Add explicit bounded generated-media option (not arbitrary unbounded number), public enum AudioInspectionPolicy: Sendable { case original, generated }; inspector inspect(at:policy:) and transport preparePlayback(url:format:range:policy:) default policy.original; ProjectSession will select .generated only for registered modelGenerated results. Use policy both inspector and transport, default.original so old callsites unchanged. Do not modify queue capture/record permission logic.
- Metadata/read limits: imported short-source defaults stay 120seconds/64MiB. Current generated-model profiles include up to380seconds, so generated inspection accepts explicit bounded limit (e.g.380sec/256MiB), and validation doesn't downgrade medium to current-Mac 6sec. Parameterize existing inspector safely if needed; no unbounded loads. Current profile backend still rejects over-profile/over-memory inputs. Existing M4 tests use tiny synthetic clips.

Lead acceptance: schema1/2/3/4 backup byte preservation; empty creation/reopen; normal WAV result captured job/precision metadata/prompt/seed actual; selection != adoption; reject/unreject/adopt nil survive reopen; wrong-document/source/UUID/path/format/hash/frames/NaN fail preserving originals; save external-change or read-only failure leaves existing manifest/asset and output; pending/interrupted recovery doesn't fabricate success; compatible source snapshot independent of later source path removal; collision no overwrite/symlink escape; generated output can become next doc's reference without Tasks overlap; image/text/old audio tests unchanged. No actual MLX/GUI/network/device. CPU fixtures label modelGenerated truthfully as fixture testing, not real generated evidence.

Read this spec, AudioCreationDraft.swift, ProjectModels/ProjectStore relevant methods and existing audio/migration tests; no full historical audit. New specification side effects must be reported before changing scope. All temp/cache/output in own granted run directories. SwiftPM sandbox denied=>stop check/report Lead, never disable/expand. No commits/docs/other task writes.

Clarifications before task dispatch: blank/whitespace prompt must reject at makeRequest; new revision must differ when content changes; malformed metadata cannot gain generated bounds just by exporting. Preserve original existing instance-level failure-injection hooks and safe publishing checks. No input/output directory scanning. DInference.AudioRequest.validate is shape-only; this current app adapter must truthfully enforce current supported shape without claiming all future models use these ranges.
