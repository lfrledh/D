# D-TEXT-PROFILES-01
Revision1 / TEXTSCALE1 / batch D-AUDIO-BACKEND-01. Source beaaaf82d845e672c6a3b661d928654affc00518; execution base full preparation SHA in dispatch. Sol/high for integrity/registration coupling. Initial+2 repairs; no recursive agents.

Goal: workbench accepts registered Qwen2.5 Instruct 4bit 0.5B/1.5B/7B/32B instead of only0.5B; preserve old default and all full checksums, no automatic download or bigger-model OOM promise. Backend qwen2 already accepts variable dimensions; resource estimate and runtime budget reject incompatible machine separately from model identity. Do not weaken flat4bit/group64 or add model families/GUI panels/downloadmanager.
Exact allowed paths: Packages/UI/Sources/DWorkbench/Models/FixedTextModel.swift; new same-directory TextModelProfiles.swift; Packages/UI/Sources/DWorkbench/State/ProjectSession.swift only text-model status/title lines; Packages/UI/Sources/UI/State/WorkbenchModel.swift only text-model chooser title; D/AppSessionFactory.swift only text registration verification (preserve new ResourceBudgetPolicy line); new Packages/UI/Tests/DWorkbenchTests/TextModelProfileTests.swift. No other code/metadata/docs/resources/locks/sharedcontracts/runtime/backend/project/permissions/signature/.git. Lead supplies pinned full resource manifests, already metadata/config/license verified, no weight downloads yet. Existing text-model.json0.5B unchanged; new text-model-1_5b.json/text-model-7b.json/text-model-32b.json are authoritative registrations, not evidence of real inference.

API: public TextModelProfiles exposes immutable registered descriptors containing stable ID, displayTitle, repository, revision, quantizationBits4, validationStatus (0.5B previouslyRealModelVerified, others registeredAwaitingRealModelValidation). verify(at:) async returns ModelReference only after exact one matching bundled manifest and full hashes. No filesystem names or caller .provenance authority. Candidate selection by bounded metadata+file layout/size is allowed for performance but final identity all manifest hashes required. Wrong/missing/extra model files never accepted, except original .download.lock and .provenance.json exact regular bounded bookkeeping. Reject symlink/root indirection/specialfile/changed-duringhash as existing or stronger; no private writes/caches. Cancellation must stop ongoing hash task before verify returns (do not detached hash leak on parentcancel). Deterministic unknown revisions fail visibly. Duplicate/invalid embedded manifest is packaging error, never fallback firstprofile. Two manifests cannot masquerade as one; list stable predetermined IDs, no model directory scanning beyond chosen model. Tests may use explicit internal injection of tiny trusted manifests for checksum scenarios; production must only use bundled registrations, tests not redefine acceptance.
Preserve FixedTextModel.title and verify compatibility as the original0.5B-only wrapper so oldtests/callers keep exact semantics. Application explicitly opts into TextModelProfiles.verify; project status uses reference revision resolved by profile title or neutral unknown label, never labels7B as0.5B. Existing bookmark key/data unchanged; model replacement identity and tasklease flows unchanged; no changing input/document/ProjectStore schema. Chooser says registered Qwen2.5 Instruct4-bit family and supported sizes; not unlimitedmodelclaim. Extra profiles have unmeasured status in user-facing modelstatus where practical. Do not call model loading in checks or use fixed memory requirement label for allhardware.

Acceptance: tiny valid manifests registration for distinctsmall/larger identities, full hash corruption/missing/extra/badmetadata, sameconfig differentweights cannot pass, unknownrevision, symlink/specialfile, cancellation bounded fixture, original0.5Bwrapper rejects differentvalidprofile, dynamictitle/bookmarkrestore path assertions, preserve all existing registration/sourcefile tests. Native UInt64/Int overflow and invalid sizes/digests in injectedmanifest rejected. Tests clearly metadatafixture only; Lead later runs oldactual0.5B checksum and new1.5B onlywhendownload available, realmodelseparately. Worker allowed swiftc -frontend -parse with ownedcache, no SwiftPM/build/GPU/MLX/GUI/network/modeldownload. Only task tree+worker-output/tmp write roots. No Git commit or spec edits. Read this spec and current APIs/rule stopsections; return ambiguities in precheck before implementation, then allowed checks/errors/processstates/results.

## Revision2 - Lead API clarification before implementation
Public immutable TextModelProfile (Sendable, Equatable): id:String, displayTitle:String, repository:String, revision:String, quantizationBits:Int, validationStatus enum cases previouslyRealModelVerified/registeredAwaitingRealModelValidation. IDs are exactly repository strings mlx-community/Qwen2.5-0.5B-Instruct-4bit, mlx-community/Qwen2.5-1.5B-Instruct-4bit, mlx-community/Qwen2.5-7B-Instruct-4bit, mlx-community/Qwen2.5-32B-Instruct-4bit; revision separately identifies exact modelversion, directory/installation notID.
TextModelProfiles.registered() throws -> [TextModelProfile] in ascending size order; profile(forRevision: String?) throws -> TextModelProfile?; verify(at: URL) async throws -> ModelReference; status(for: ModelReference?) throws -> String is allowed helper. Both list/query/verify validate bundledregistrations, malformed or duplicate descriptor/manifest throws InferenceFailure.invalidRequest with explicit packaging-error message, never returns fake emptylist/default. Unknown revision query returnsnil; unknown revision injected into known profile manifest is rejected by expected descriptor match. Tests also cover successful fake session validator yielding unknownrevision then neutraldisplay; `.provenance.json` never supplies identity. Internal small testmanifest injection may replace files/sizes/hashes while keeping known expected descriptor repository/revision; tests notproductionauthority.
Titles: Qwen2.5 <size>B Instruct · 4-bit. Verified file status: title + ' · 文件已校验'; unmeasured profiles append ' · 推理待本机验证'; old0.5B no such suffix. Unknown revision neutral '文字模型 · 版本未登记', nil '尚未选择文字模型'. Initial/cleared prompt and chooser title can use '选择已登记的 Qwen2.5 Instruct 4-bit 模型（0.5B／1.5B／7B／32B）'. No new UX decisions or additionalpanel. FixedTextModel.title/verify old0.5-onlywrapper remain. Original0.5 model checksum fixture remains compatible. New1.5 actual download was blocked by auto-review H12, do not attempt download or mark physicalvalidation passed. PublicAPI and meanings frozen, internal hashing/selection organization remains Worker choice.
