# D-ALIGN-UI-01 / implementation-r1 / ALIGN1

Status PRECHECK only until Lead IMPLEMENT. Batch D-ALIGN-01; source aa277f965629591a23f80f7f4baf3b081cd8de2a; execution base launch metadata (contains this record). run run-20260913T104905Z-ui. gpt-5.6-terra / medium. Current backend workers implemented typed capabilities; full combined checks pending. No inference/UI test claims yet.

## Scope and exact interfaces
Build native, clear existing parameter controls only. No new workspace, state/store, backend, model catalog, UI editor lifetime/id changes.

1. GenerationInspector replaces fixedSettings static512 display with editable width/height using existing WorkbenchModel.imageSettings, imageCapability, imageConfigurationError, executionRecommendations. Existing imageProfile keeps model defaults only: never use it as current user setting. Show actual settings width/height, fixed capability.steps/guidanceScale, known range and32-step, not memory-based disabling. A few 512/768/1024 dimension presets permitted only if current capability accepts; self-render arbitrary current values even if invalid. Selecting a supported preset or manually editing a dimension is explicit user adjustment and may choose current capability.profile (not a quantization/model switch), never during rendering or machine change. Each action reconstructs immutable ImageGenerationSettings with both current dimensions and chosen profile; width change preserves height. Invalid saved unknown profile/values remain visible until explicit user change; diagnostic visible and generate uses existing canGenerate. No clamp/onAppear/onChange silent normalization. Use accessibility IDs image-width, image-height, image-settings-error and parameter section. Controls must remain usable in narrow inspector with vertical layout, scroll and existing pinned generate button; do not change external layouts.
2. TextWorkbenchView adds modifier exactly:
`public func generationControls(capability: TextExecutionCapability?, recommendation: ExecutionRecommendations?, configurationError: String?, onChange: @escaping (TextGenerationSettings) -> Void) -> Self`.
Store injected presentation values/callback only. Read current settings from session.document.generationSettings; call callback for manual changes, preserving other values; no direct session business mutations. Show input/output token numeric controls and explanatory short text (token不是字数，输入含改写包装和模板；真实超限报错不截断). Do not use character count as estimate. Display current supported max input/output from capability; nil capability means no configured controls, not presumed high limits. Allow unfinished/out-of-range values to remain, show configurationError; no clamp. Explicit reset to short preset is user action using 2048/256 iff accepted by current capability; unknown profile values remain until explicit adjustment. Typed format .number, no arbitrary integer casts. IDs text-input-limit, text-output-limit, text-settings-error. Same controls in complete/parameters modes via existing rewriteControls; editor-only mode stays focused editor. No new `.id`, selection reset, IME replacement or keyboard shortcut.
3. AudioCreationView adds modifier exactly `public func capabilitySummary(_ capability: AudioExecutionCapability?) -> Self`. Render short actual deployed profile name/range/sample rate and operation support in its existing parameter region for complete/parameters; no model menu expansion. SA3 small instance cannot show medium executable. Nil is '当前工作环境未提供生成能力声明' (not claiming hardware too small). MRT2 approximate note fidelity means style/accompaniment may differ; no strict chord/locked audio claim. Do not change parameters, validation, operations, microphone/playback or sessions.
4. Focused view tests in new ExecutionSettingsViewTests; test same callbacks/settings are used and descriptions don't advertise unprovided engines. Follow existing UI package tests; do not fabricate real interaction evidence. Lead attaches modifiers and runs CPU/native checks. If meaningful callback test requires inspecting private controls, propose tiny internal pure projection helper in owned view file rather than mirrored implementation or public framework.

## Exact writable paths
- Packages/UI/Sources/UI/Views/GenerationInspector.swift
- Packages/UI/Sources/UI/Views/TextWorkbenchView.swift
- Packages/UI/Sources/UI/Views/AudioCreationView.swift
- Packages/UI/Tests/UITests/ExecutionSettingsViewTests.swift

No other files including task docs. Especially no WorkbenchView/WorkbenchModel/ProjectSession/Store/Models/TextDraftSession, runtime, App, CLI, scripts, dependencies, manifests/lock, signature/permissions, source tree. Lead handles modifiers at callsite. No recursive delegation or commit/Git writes.

Read AGENTS current safety, stage docs/tasks/D-ALIGN-01.md current implementation/behavior table, own views and existing nearby UI tests only. New input APIs are in current worktree; don't scan all historical tasks. Instruction omission/ambiguity report to Lead before implementation.

Worktree /Volumes/CodexProjects/Codex/D-Worktrees/D-ALIGN-UI-01; output /Volumes/CodexProjects/Codex/D-Development/AgentTrials/D-ALIGN-01/run-20260913T104905Z/ui/output; tmp /Volumes/CodexProjects/Codex/D-Development/AgentTrials/D-ALIGN-01/run-20260913T104905Z/ui/tmp. workspace-write implicit cwd+only output/tmp; network false. Explicit /Applications/Xcode.app/Contents/Developer/usr/bin/git; no xcrun/system Git wrapper. After IMPLEMENT allowed editing and explicit Swift frontend parse owned files, no full build/SwiftPM/GUI/inference/model/network/dependency operations; Lead serial tests. Optional temporary/output tool env must explicitly use own tmp; no default py_compile; unknown refusal/runtime warning stop/report, no bypass. PRECHECK no edits/tests; return task/revision/run/root/common/HEAD/branch/paths/acceptance.

Initial plus max2 targeted repairs, max900s each, no budget reset. Return paths/diff, behavior, performed checks vs pending, process state and deviations; evidence own output only, task record Lead-owned.
