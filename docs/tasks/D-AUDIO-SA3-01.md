# D-AUDIO-SA3-01: local audio provider

Task D-AUDIO-SA3-01 / spec_revision1 / contract AUDIO1 / batch D-AUDIO-BACKEND-01. Source beaaaf82d845e672c6a3b661d928654affc00518. Execution base is preparation commit containing this spec, sent with full SHA. Implementation gpt-5.6-sol/high: numerical integration and file safety warrant higher reasoning than routine scripts. Lead handles Swift/runtime/shared contracts and integration, Worker implements actual Python provider.

## Scope
Implement a real offline SA3 adapter (generate, variation, inpaint), safe request/PCM handling and CPU tests. Do not download/install/run MLX/GPU or accept license terms; model permission is pending independently, not a reason to omit implementation. Vendor code is MIT, pinned779434a908193105335fd8d833418603625b2859. No weights are bundled. Only these allowed new files:
- Backends/Audio/Python/d_audio_backend.py (CLI)
- Backends/Audio/Python/d_audio_contract.py (stdlib validation/PCM/files)
- Backends/Audio/Python/d_audio_sa3.py (real MLX adapter)
- Backends/Audio/Tests/test_audio_backend.py (CPU tests)
No other edits including Vendor, specs/docs, Swift, project/lock/settings/signatures/model tree/source/.git. No subprocesses except controlled CPU CLI tests with timeouts; no child agents or background jobs. Initial delivery+2 repairs.

## Frozen JSON contract and ownership
CLI --request ABS_JSON --job-directory ABS_EXISTING_EMPTY_DIR --model-directory ABS_DIR --profile sm-music|sm-sfx|medium --manifest ABS_JSON --vendor-directory ABS_DIR; optional --inspect (validate/report readiness without model initialization). Paths explicit, no scanning or auto-download. stdin unused. Separate trusted launch configuration from untrusted generation request.
Request schemaVersion integer1 (bool forbidden), runID canonical UUID, operation generate|variation|inpaint, prompt Unicode <=1MiB, durationSeconds finite positive, seed integer0...2^32-2 (SA3 sampler uses seed+1), steps integer1...100, guidanceScale finite1...15, strength finite0.01...1. Generate requires no source/editRegion and strength1; variation requires source and no region; inpaint requires source + editRegion. Unknown keys/types rejected, no NaN/Infinity, duplicate JSON keys rejected, max request1MiB. Output provenance freezes exact request, profile, source/manifest/vendor revision and precision.
source={path:absolute,sha256:64lowerhex,frameCount:positiveInt,sampleRate:44100,channels:2}; editRegion={startFrame:Int,endFrame:Int}, half-open within source. durationSeconds must match source.frameCount/44100 within half sample for edit modes (no silent trim/resample/channel conversion). Source only RIFF/WAVE LPCM stereo44100 int16/int24/int32 or float32, bounded<=512MiB and profile duration. Validate actual RIFF chunks/declared lengths/format/alignment/data count, finite decoded samples; reject truncated/unsupported/symlinks/special files, preserve raw original bytes. Source frames and digest verified before/after reading, copy/freeze input only under task job dir. No ffmpeg/network/library auto conversion. Python arrays can use stdlib array/struct; no numpy import during CPU validation.

Manifest schemaVersion1, repository stabilityai/stable-audio-3-optimized, revision 40lowerhex; profiles sm-music/sm-sfx/medium map exact known DIT+matching codec. files=[{path:MLX/plain-filename.npz,size:positiveInt,sha256:64lowerhex}]. Required per profile: dit_sm-music_f16.npz or dit_sm-sfx_f16.npz or dit_medium_f16.npz; t5gemma_f16.npz; same_s_decoder_f32.npz/same_s_encoder_f32.npz (same_l for medium). No arbitrary imports from manifest, no path traversal/symlinks; weights regular, size+SHA checked using bounded reads before loading, metadata changes during verification fail. Missing/corrupt/version/type mismatch fails before MLX import. Additional unrelated files in model directory are not traversed/executed; only validated paths read. Backend never writes model/vendor/source. Config explicit license acknowledgement is host responsibility before invoking actual inference; --inspect may run without model use.

Known model caps: sm-music/sm-sfx <=120s, medium<=380s; local acceptance planned6s/8steps/seed42/guidance1, not global product upper bound. Estimate from required weight/component peak + duration/step workspace with overflow/bounded arithmetic; conservative, labeled estimate not measured. CPU validation accepts larger profile metadata without pretending real support is measured.

Success: job/output.wav is stereo44100 IEEE float32 master, finite, exact requested frame count round(seconds*44100); model uses FP16 DiT/T5 and FP32 encoder/decoder, no quantization/downcast16 output. Inpaint outside exact [start,end) must preserve decoded original PCM sample values: copy source PCM and replace only region with generated samples, no global normalization/fades. Explicit no crossfade first version (potential audible seams documented). Full output container bytes need not match original. Quantized latent mask may cover wider range; reject edit region rounding to empty latent interval, report both requested frames and effective model latent interval. Variation approximate reference, no exact melody guarantee. Never claim symbolic MIDI/lyrics controls.

Write .partial output only within owned job root, validate by independent reader before exclusive atomic publication (no overwrite). Manifest/result JSON and outputs cannot overwrite existing files or touch input/model/vendor or parents; output error/consumer error not success. Published valid artifact survives later stdout failure, diagnostic records location if possible. No cleanup scanning; only created temporary paths may be removed. Error exit2 invalid request/config/output failure; exit1 actual engine failure; exit130 cancel. stdout JSON lines protocol schema1: phase/progress, then result artifact+metadata or error {kind,message}; stderr diagnostics separate. All stdout writes/flush failures need controlled nonzero exit after full cleanup (do not use os._exit or swallow arbitrary exceptions); use safe reporting if stderr broken too. Main CLI finite timeout is held by Swift/Lead; SIGTERM/SIGINT set cancellation flag checked per stage/step, exit only after current synchronous MLX operation completes; host may kill own timed-out process and await actual exit. Release references, mx.synchronize/clear_cache on managed exits; do not claim OS process exit proves leak-free long-running reuse.

## Real implementation guidance (not exact code prescription)
Use pinned Vendor models.defs APIs and official reference scripts/sa3_mlx.py in readonly evidence source to preserve math. Do NOT call its main() or weights.ensure_local. Tokenizer silently slices max_len256 upstream: explicitly inspect tokenizer length and reject overlong before encode; no hidden truncation. Use same conditioner/T_lat=ceil(seconds*44100/4096), SAME padding, noise keys and sample_flow_pingpong; on_step propagates cancellation and output failures. Load text/codec/DiT in separate stages, clear references/cache; decoder/source encoder FP32. Capture phase timings and MLX peak/active/cache labeled allocations, not RSS. No LoRA/negative prompt/training/gradio/cloud server. Unknown implementation questions go to Lead, no invented model behavior.

## Frozen acceptance
CPU tests must cover all three operation contracts, malformed/duplicate/bool/version/Unicode/large-seed/nonfinite data, empty latent interval, non44100/multichannel/resample refusal, integer/float PCM decoding, source digest/frame mismatch/changed file, model manifest corruption/missing/untrusted path, path overlap/symlink/special file, no overwrite/atomic output/failed publication, sample-exact preservation outside region, CLI invalid/inspect/full process exit/stdout broken pipe (controlled fixture). Successful mock inference is only orchestration fixture, never real model acceptance. Test only task temp/output, tempfile uses D_TEST_TEMP_DIR/TMPDIR, no private user input.
Lead runs actual CPU suite + independent counterexamples and later real6s model tests when license/resource conditions allow; generation/repeated/cancel-next/variation/inpaint independently inspect WAV, preservation, metadata, performance and memory. If legal/physical gate remains, candidate status stays real-model acceptance pending. Do not lower tests to mark stage complete.

## Runtime boundaries
Precheck first; task tree plus own worker-output/tmp only, networkfalse, no shared.git. Use exact Xcode git path supplied. Read this spec, pinned relevant definitions/ref script, AGENTS boundaries and workflow error/stop sections only. No full history. CPU stdlib unittest permitted; syntax with tokenize.open+compile(...dont_inherit=True), never default py_compile; PYTHONDONTWRITEBYTECODE=1 and unique tmp. Imports of target only in behavior tests, never conflate with static compile. No installed MLX/weights access by Worker. Report actual files/commands/evidence/remaining gaps/errors/owned process state; don't modify this record.

## Revision 2 - pre-implementation clarification by Lead
Effective before implementation; previous precheck exposed ambiguity, not a Worker failure. Original execution base2670e81; new full base is this preparation commit in dispatch.

1. All stdout objects: schemaVersion integer1, type string, runID canonical request UUID when validated (null only invalid/unreadable request). progress: phase in validating/encodingText/encodingSource/denoising/decoding/publishing/cleanup, completed/total nonnegative integers, completed<=total, total>0. Emit meaningful phase boundaries, no fake time prediction. result: artifact={path:absolute path to job/output.wav,sha256,byteCount,frameCount,sampleRate:44100,channels:2,encoding:"float32"}, metadata object. inspection: profile, modelRevision, requiredWeightBytes, estimatedPeakBytes, estimateKind:"conservative-unmeasured", capabilities={generate:true,variation:"approximate",inpaint:"exact-outside-region-after-float32-conversion"}. error: kind invalidRequest/configuration/output/engine/cancelled and message nonempty; process exit determines success, never event alone. Exactly one terminal event unless output itself fails. No MLX import on inspection.
2. Only permanent job files: frozen-source.wav (edit modes; preserve raw bytes), output.wav, result.json. result.json is the same result object, exclusively atomically published after WAV verification and before stdout delivery. Its metadata contains request snapshot, profile, modelRepository, modelRevision, weightManifest (paths/sizes/hashes), vendorRevision, precision={dit:"float16",text:"float16",encoder:"float32",decoder:"float32",master:"float32"}, timingsSeconds (stage names; finite >=0), mlxAllocations (explicit active/cache/peak fields where measured, not RSS), requestedRegionFrames and effectiveLatentRegion where applicable. Request snapshot omits source absolute path; retain source sha256/frame/rate/channels and operation inputs. No other sidecars. Failures after a WAV is published leave it intact even if result.json cannot publish. Own partial names may be unique; never overwrite/cleanup unknown files. No atomic-two-file guarantee.
3. Prompt limit means UTF-8 bytes. Request file max1MiB still applies to full encoded JSON (so escaped JSON may reach limit earlier).
4. Manifest items must be exactly the five required profile files, no extras/duplicates. Expected initial model revision da6edc54ddba10bfd79a077102ded687f80e882b (selected optimized weight commit); reject other revisions until explicitly registered. SHA/size from trusted supplied launch manifest, not generation input. Do not invent installed model availability.
5. vendor-directory is root with provenance.json and models/defs. Verify repository https://github.com/Stability-AI/stable-audio-3, revision779434a908193105335fd8d833418603625b2859, and exact shipped provenance.json bytes against the SHA from this execution tree embedded in adapter. Then hash every listed regular file, safe relative paths, before inserting vendor root in sys.path. No extra vendor directory imports or upstream main/downloader. It is trusted installed executable code, not arbitrary user-provided metadata.
6. All explicit CLI paths absolute; reject symlink components, special files, ../ components and overlap based on normalized paths. job must be separate from model/vendor and neither contain nor be contained by either. Request and manifest files must be outside job; source outside job; job cannot contain request/manifest/source. Reject model or vendor writable-output overlap; model/vendor may share an ancestor. No symlink traversal through parent dirs; use lstat/O_NOFOLLOW and read identity checks. Do not alter original permissions. Require job empty before creating owned files. Source may share ordinary parent with request, but not be inside model/vendor. Config files bounded1MiB. Do not recursively scan parents/model trees.
7. Estimate (bytes) = 2 * largest of five required weight sizes + 1GiB + ceil(durationSeconds)*64MiB. This deliberately reserves phase loading/transient copies and duration workspace; not physical availability or guaranteed bound, pending real calibration. Report unmeasured; steps change compute/time, not this peak estimate. Host must compare to task budget before GPU starts; adapter never silently lowers duration/precision. This is a policy estimate, not measured memory.
8. Integer32 PCM -> float32 master can round decoded values. Preserve original raw source unchanged and record sourceSampleEncoding plus sourceToMasterConversion in metadata. Outside inpaint region must equal explicit float32 conversion of decoded source, never claim int32-bit-exact preservation. int16/int24 are exactly representable in float32; finite float32 source unchanged outside. Add an int32 boundary counterexample. No resampling/global normalization/crossfade.
9. CLI ordinary python -B path launch is supported; adjacent contract/adapter imports allowed. Host will pass request outside empty job output directory. Model terms remain pending; no weights/GPU calls under Worker scope.
