# D：项目 → 模态 → 创作的 UI 层级方案

日期：2026-09-11。任务 D-UI-HIERARCHY-01，设计修订 NAV1；用户已明确新的层级方向，本轮交付结构方案、可点击草图和下一阶段工作包，尚未执行 SwiftUI 产品替换。下列布局和行为细化为 Lead 设计决定；不把原型点击当作真实产品验收。

## 1. 目标与当前基线

用户先决定“在哪个项目工作”，再决定“做哪类内容”，最后进入该类创作的文档、候选与参数。项目仍是保存、共享资产和任务归属的边界；模态是项目内的工作区，不是另一份项目或物理目录。模型是执行资源，不是内容导航的一级分类。

已核实源分支 codex/inference-foundation，基线 31b351240778f005c0ef5cb51b0e0d1e6b0db3b0；相对前代码基线633150477de90d30c1f44c915b1f10e13acd246d仅状态/待办文档。AW1隔离候选0295618cb851511c8aa3c207a3a4b887913d7501，服务受测d6b7d1ea5df72730ed9c198d8df56d5c17bb6d5c，源码未接入源。保护对象：个人scheme未暂存修改、普通D、用户作品、旧任务与证据。

当前差异有代码证据：

- `D/WorkbenchBootstrap.swift:start` 普通启动调用restoreLastProject；与新要求“先显示项目选择”不同。
- `Packages/UI/Sources/UI/Views/WorkbenchView.swift:ArtworkSidebar` 把全部作品、跨模态文档和当前候选放在一起；项目顶部又有图像专用导出/条件复用。
- `Packages/UI/Sources/DWorkbench/State/ProjectSession.swift:visibleAssets/showAllArtworks/selectDocument` 把候选范围、总览与导航耦合；切换前已有保存/文字状态门槛，不能绕过。
- `Packages/UI/Sources/UI/State/WorkbenchModel.swift:revealAsset` 会跳至资产来源文档，不能直接用于只读资产预览。
- `Project/ProjectModels.swift` 源版本的ProjectDocumentKind只有image/text；文本正文是文档数据，不是文件型ProjectAsset。音频类型/存储存在于AW1候选，不可把源与候选能力混写。

只读Sol/high审查核对同一源SHA，给出7项导航/状态差异；非实现者没有执行产品测试。运行与可观察模型/权限见本任务外部回执。

## 2. 固定的信息层级

```text
D
├─ 项目选择（普通启动首页）
│  ├─ 新建项目 / 打开项目
│  └─ 最近项目（只展示已记录项目，不扫描磁盘）
└─ 当前项目：名称 + 返回项目选择
   ├─ 顶部模态栏：图像 | 文字 | 音频
   │  └─ 当前模态的工作区
   │     ├─ 创作列表：本模态的多份工作 → 当前编辑区
   │     │  ├─ 当前输入、画布/正文/声音预览
   │     │  ├─ 当前创作的候选 → 比较 / 采用或拒绝
   │     │  └─ 当前操作参数 → 兼容模型 / 高级参数
   │     └─ 资产面板：本项目资源，默认仅本模态
   │        └─ “也显示其他模态” → 只读预览 / 明确后续动作
   └─ 项目任务入口：各模态运行状态，保留来源

应用菜单：模型库、设置。它们是共享资源/应用配置，不与模态并排。
```

没有作品的已支持模态显示“新建图像创作／新建文稿／新建声音创作”，切过去不自动创建文档或写入空任务。未来视频、歌声、哼唱识别只有可用工作流落地后才出现对应入口；不先填一排灰色占位。音频栏是本轮设计目标，真实发布时受AW1产品验收门槛控制，不能仅因CLI可生成就默认启用。

## 3. 默认摆放

|位置|放什么|明确排除|
|---|---|---|
|项目顶栏|返回项目、项目名、项目任务状态|当前模型的大标题、跨模态混用的生成/导出按钮|
|其下独立模态栏|图像、文字、音频；可见选中状态|“模型”“设置”“全部作品”这种不同层级标签|
|左侧区域|“创作／资产”两种面板切换；创作只列本模态文档，资产提供简单筛选|候选、全部文档、项目列表全部挤在一个长列表|
|中央|当前文档编辑器；标题/保存状态；局部编辑、比较、导出动作|因查看外模态资产而自动替换当前编辑器|
|中央下方|当前文档候选；文字原文/改写、图片候选、声音候选，按模态呈现|所有项目资产混成候选；资产预览自动采用|
|右侧可收起参数区|当前操作的提示、参考、参数、兼容模型、主操作；少用参数折叠|让文字出现图片尺寸；让图像出现音频区间；只剩一个选项仍显示选择器|

“创作／资产”是同一模态内的面板入口，不是更换项目。参数始终跟随当前创作，资产详情在资产面板内预览。用户从模型选择进入的列表先筛兼容模型，管理/安装进入现有共享模型库；不复制三份模型库。

Liquid Glass采用Apple原生导航、工具栏、按钮材质；画布、正文、波形及资产内容保持清晰、中性。避免玻璃叠玻璃，降低透明度/减少动态/提高对比度后层级仍可辨认。依据：[Apple Meet Liquid Glass](https://developer.apple.com/videos/play/wwdc2025/219/)导航与内容分层、可访问性部分（2026-09-11核对）。HTML草图表达摆放与切换，不证明原生材质的最终效果。

窄窗口先收起参数区为可打开的面板，再折叠左侧列表；主要编辑区、模态和回项目入口始终可达。工具栏按可用宽度重排/收纳，不靠窗口边缘裁切。实现阶段保留已验证最小窗口860×580作为第一组基线，另测1024和1440宽度；具体阈值由真实控件测量决定，不机械照抄草图。草图360px适配只是会话内预览，不承诺手机产品。

## 4. 资产的最小规则

1. 范围永远限定当前项目。默认“本模态”，勾选“也显示其他模态”扩展为本项目全部资源；切换模态重置为本模态。每行显示名称与类型，计数反映过滤后集合。
2. 过滤/预览不调用showAllArtworks、revealAsset或采用操作，不改变activeDocumentID、主画布选中候选、生成参考或模型。跨模态预览也不自动开启生成或触发转换。
3. “打开来源创作”仅在来源文档确实存在且对应编辑器可用时出现；无来源或能力不足仅显示可用预览及明确原因。“用作参考”必须由当前操作的能力和格式校验决定，不按文件后缀推断。
4. 媒体按已记录且可识别的mediaType分类，未知类型保留“其他/未知”，在全部范围可见。不通过所在文档的模态误判附件类型。
5. 已保存文字文稿可用标为“文稿”的文档引用行投影到文字资源列表；它仍是文档记录，不伪装成文件型ProjectAsset、不编造路径/摘要。素材文件与可编辑文档使用不同ID种类，打开和导出分别校验能力。此为少量展示适配，不增加持久化资产平台。
6. 本轮不做复杂搜索、标签体系、集合、跨项目素材库、媒体转换或批量整理。原始资产与导出快照仍按既有规则保存；过滤不迁移、不复制、不删除任何媒体。

## 5. 切换与保存行为表

|事件|规定行为及反例|
|---|---|
|普通启动|显示项目选择，保留最近记录；不自动打开上次项目、不因显示列表读取全部项目内容。最近项仅来自成功打开/保存过的项目，名称缓存和授权记录沿用受控存储；缺盘可见，不静默删记录。|
|用户明确从Finder打开项目|作为明确的打开动作可直接进入该项目；不是普通冷启动自动恢复。重复打开同一项目不重建会话。|
|选择模态/文档|先通过现有保存及离开门槛，成功后一起切换可见模态、文档和编辑器。保存失败保持原界面；不能“标签已是音频、正文还是文字”。|
|文字生成或候选未处理|保留现有拒绝离开的门槛，提示先取消/接受/拒绝等明确动作；不默默丢弃、接受或承诺目前尚未实现的后台候选跨控制器保存。|
|图像/音频生成中切换|仅在原服务允许的安全边界导航；任务条件和来源冻结，结果回原文档，不抢当前焦点。切换不是取消；取消仍由明确操作触发，释放完成才结束。|
|切回已访问模态|恢复本项目该模态最近有效文档；ID无效时选择现存文档或空态。会话内导航偏好与项目内容分离，不为导航新增一次媒体迁移。当前单activeDocumentID仍由服务拥有；不再造一份并行权威状态。|
|资产预览/过滤|编辑器/选区/候选不变；关闭预览回原现场。选择外模态文件不等于能送进当前模型。|
|A→B→A或迟到文件面板结果|用项目/文档/导航代次检查操作归属，单纯比较当前UUID不足。失效操作拒绝，不写回新工作现场。|
|返回项目选择/换项目|走现有保存、取消确认及drain门槛；未完成不能先拆掉编辑器。数据/资源处理通过后才进入项目选择。|
|快捷键与菜单|与可见模态/文档的同一命令状态绑定；不能顶部为音频而⌘Return仍生成图片。文件面板/项目切换期间沿用禁用保护。|

不在这次导航设计中修改推理精度、模型参数真实性、文本Unicode选区或候选采用语义。旧启动自动恢复测试因新用户需求需要明确更新；项目安全恢复、冲突保护和输入法测试不因此删除或降低。

## 6. 保留、调整和替换

|决定|现有部分|处理|
|---|---|---|
|保留|ProjectStore、DRuntime、各后端、项目/文档/资产稳定ID|复用既有保存/任务事实；不拆新包、不为界面重排物理文件|
|保留并嵌入|TextWorkbenchView、TextSelectionEditor、ArtworkCanvas/ArtworkComparison、GenerationInspector|保留已验收编辑行为；图片专用操作放回图像内，避免重写输入法桥接|
|替换组合方式|WorkbenchWelcome、ArtworkSidebar、WorkbenchView公共工具栏|项目选择页、模态壳层、本模态文档列表、独立资产浏览器|
|调整|WorkbenchBootstrap.start、WorkbenchModel门面、ProjectSession导航协调、DApp菜单|首页启动策略、明确切换结果、当前模态命令路由；共享修改由Lead单一协调|
|保留未通过状态|AW1音频视图/已通过服务|新导航可规划容纳音频，但旧VIEW三缺口和修复预算0不改写。旧VIEW未合入；不得借本任务名称追加其第三轮。音频接纳仍需明确有界收尾和H09/H15。|

## 7. 下一阶段工作包与出口

阶段目标：让用户从项目页进入任一已验收模态，找到自己的创作与项目素材，切换后保住工作现场。当前设计完成不等于下面代码工作已执行。

|顺序/负责人|工作与文件所有权|依赖/验收|
|---|---|---|
|NAV-C Lead先行|核对新完整源SHA，冻结CreatorMode、导航结果、当前模态快照、每模态最近文档、AssetScope、资源行ID和打开能力；最小协调WorkbenchModel/ProjectSession/Bootstrap/DApp。新UI文件名由任务规格列出，不能Worker自由改整个目录。|保存失败不跳、候选/生成离开门槛、空模态、A-B-A、命令路由、模型能力测试。先形成可编译的共同契约，重要实现由非实现者复核。|
|NAV-S Worker（建议Sol/high）|只实现项目选择/顶部模态/工作区布局的新视图和直接视图测试；WorkbenchView最终装配由Lead。|依赖NAV-C；无存储/运行时/签名写权。长中文/emoji名称、窄窗、空态、返回项目、焦点与减少透明度。|
|NAV-A Worker（建议Terra/medium，必要时按风险调整）|只实现资源投影/本模态与全部筛选/独立预览的新文件和局部测试。|依赖NAV-C；与NAV-S没有共享写文件，可真正并行。媒体/文档ID分开、未知MIME、来源缺失、跨模态不导航、不改采用/参考。|
|NAV-I Lead串行|装配两个候选与现有图文编辑器，核对菜单/快捷键、保存及旧项目回归；具体SHA组合检查。|CPU→独立无签名App编译→可隔离真实GUI。未验收新路径不默认替换现有产品；不能把HTML检查当SwiftUI检查。|
|AW1收口（独立门槛）|在原D-AW-VIEW-01明确记录限定修订/额外授权，再处理来源区间、禁用原因和可靠布局验证；复用本次已过服务，配合新壳层。|不重置初交/两修/Lead失败历史。H09真实录音、H15新音频工作台与沙盒provider访问仍待实测。图文新导航不必等待音乐/识谱等研发。|

只在任务接口和文件所有权就绪后启用0—2实现Worker；不用更多调查任务冒充并行实现。每任务初交/两修/一次有界接管按现行规则记录，旧任务剩余预算不刷新。源分支仅接纳具体受测组合；不动个人scheme，不改签名/模型/依赖。本轮没有自动启动这些实现任务，也不推送设计证据。

本阶段不扩展完整写作、音乐模型、手机、PNG扩容、全媒体资产平台或高级节点。音频AI闭环接纳后按MUSIC_ROADMAP继续受控音乐/歌声；HUM哼唱转谱与来源配方仍有独立依赖出口，不以文本高级功能全部完成为前置。

## 8. 本轮证据与恢复

外部运行目录由 `AgentTrials/D-UI-HIERARCHY-01` 最新本任务run的final-receipt.json给出：源/候选完整SHA、只读模型上下文、审查结果、原型文件摘要、实际检查结果、个人scheme及普通D保护。大日志和截图不入Git。原型只是示例内容，模拟导航无网络、无模型、无项目写入；普通D未启动/关闭/替换。

新需求覆盖ADR0007“模态不作为项目内一级工作区”的旧呈现选择；项目保存边界、文档独立、资产共享和任务归属继续有效。ADR和总体规划只追加替代关系，不倒改2026-09-07的历史决定。后续Lead先核对源码状态与本计划当前修订，再从契约准备开始，不从会话草图直接跳到真实作品迁移。

本轮实际证据目录：`/Volumes/CodexProjects/Codex/D-Development/AgentTrials/D-UI-HIERARCHY-01/run-20260910T233410Z`。只读审查线程01a08dac-478a-70e1-acdf-e6d0f59a3a11，可观察gpt-5.6-sol/high/read-only，与请求一致；无构建/设备操作。草图检查18组组合（3模态×3宽度×浅/深色）和7项导航交互通过，0脚本异常；仅无界面浏览器结构检查，非D产品验证。草图SHA256 `fe9c5194cfe9118fade01b50c829b7d9f972bcbbdb1dd9bef74398fbfcbaf8d5`，原件副本和截图位于prototype。后续产品任务规格需从新源HEAD签发，不把本设计SHA永久硬编码。


## 2026-09-11 实施授权 NAV2 / 批次 NAV-20260911

用户批准上述结构实装、测试通过后推送，并当前确认普通D已保存退出、其他AI/GPU空闲、隔离测试窗口可用。源基线fee074c70fb39bd9a99d146b708d6d80fd2d820e，集成树D-UI-HIERARCHY-01；AW1服务和旧VIEW候选仅保留历史合入，未因此接纳。NAV2包含一次针对旧VIEW三项缺口的限定Lead收尾，旧预算/失败历史不刷新；重要Lead差异由非实现者检查。

冻结契约NAV2：CreatorMode为项目内展示枚举；真正activeDocumentID由ProjectSession拥有。导航先检查编辑/候选门槛并保存，再提交可见mode；空模态不改原文档、不创建作品，编辑器不显示其他模态。异步操作捕获上下文，模式切换期间禁止重入；每次成功导航增加代次。资产浏览不调用selectAsset/revealAsset/showAllArtworks。媒体与文稿行ID分离，按已记录MIME分类，unknown不推测；打开来源是显式动作。普通启动项目选择，Finder打开保持明确动作；最近项只取已记录授权，无扫描。

所有权：Lead修改共享ProjectSession/WorkbenchModel/WorkbenchView/Bootstrap/DApp及契约/验收/全局文档。NAV-S只写新壳层视图和其测试；NAV-A只写资源投影/视图/其测试。两个Worker不改公共契约、旧代码、任务规格、存储或运行时。各自初交+两次修复，允许一次有界Lead接管，网络关闭，无GPU/GUI/依赖安装；受限CLI预检后才IMPLEMENT。重构建/原生GUI由Lead串行独立目录；未通过路径不默认接管已验收功能。

验收：导航保存失败留原现场；文字生成/候选不得隐式丢弃；空模态、返回/重新打开、Unicode、A-B-A和菜单路由；资产跨模态浏览不改编辑状态/采用/参考，未知媒体/无来源可见；860×580、1024/1440及长中文emoji布局；图文已有行为回归。音频VIEW先修源区间/禁用提示/可信布局后非实现者审核；真实音频应用能力单列，不以合成音频证明。证据放本任务run-20260911T001411Z-implementation；用户scheme和普通D摘要前后核对。完成具体SHA组合验收后源固定SHA快进与复验，再按授权推送工作分支。
