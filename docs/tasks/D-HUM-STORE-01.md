# D-HUM-STORE-01 原声与项目持久化

spec_revision=1; batch_id=D-HUM-ENTRY-01; contract_revision=HUM1. Worker model gpt-5.6-sol/high. Worktree /Volumes/CodexProjects/Codex/D-Worktrees/D-HUM-STORE-01. Evidence/output root is master run/store, writable only this worktree plus store/worker-output and store/tmp. No other writer in these paths.

Allowed files (only):
- Packages/UI/Sources/DWorkbench/Project/ProjectModels.swift
- Packages/UI/Sources/DWorkbench/Project/ProjectStore.swift
- Packages/UI/Sources/DWorkbench/Audio/AudioMediaInspector.swift (new)
- Packages/UI/Tests/DWorkbenchTests/AudioProjectStoreTests.swift (new)
- Packages/UI/Tests/DWorkbenchTests/AudioMediaInspectorTests.swift (new)
- Packages/UI/Tests/DWorkbenchTests/ProjectMigrationTests.swift (only existing expected-current-schema literal adjustment, preserve protection assertions)
- Packages/UI/Tests/DWorkbenchTests/ProjectStoreTests.swift (same limited schema literal adjustment)
Do not modify shared AudioTypes.swift, UI/transport/session files, or other tests. If existing exhaustive switch outside scope blocks compile, report path and minimal need; do not fix without revised scope.

Implement schema4. ProjectManifest adds pendingAudioCaptures:[AudioCaptureReservation], default[] decode old. ProjectDocumentKind adds audio; ProjectDocument.audioDraft:AudioDraftDocument? (defaultnil). MediaMetadata.audio:AudioAssetMetadata? defaultnil. Old schema1/2/3 migrate preserving exact project.vN.backup.json before publication; never overwrite differing backup, preserve original media. Existing text/image checks remain, audio mutually exclusive with textDraft/image-specific refs; metadata kinds cannot masquerade; original audio jobID nil role original, relative path Audio/<canonical UUID>/source.wav or source.caf, actual assetID equals pathUUID. Pending capture path Audio/<reservation UUID>/source.caf; not also registered asset. Pending metadata exists even if file absent/partial; opening does not auto-play/delete/import. Validation fully checks metadata bounds, sha lowercase64hex, frame ranges, notes/name budgets, IDs and revisions.

Frozen public API (overloads/helpers inside allowed files free):
AudioMediaInspector.inspect(at: URL) throws -> AudioInspection
ProjectStore.importAudio(at: URL, name: String, origin: AudioOrigin = .importedFile) throws -> ProjectManifest
ProjectStore.reserveAudioCapture(name: String) throws -> AudioCaptureReservation
ProjectStore.audioCaptureURL(id: UUID) throws -> URL
ProjectStore.finalizeAudioCapture(id: UUID) throws -> ProjectManifest
ProjectStore.saveAudioDraft(_ draft: AudioDraftDocument, documentID: UUID, expectedRevision: UInt64) throws -> ProjectManifest
ProjectStore.exportAudioClip(documentID: UUID, range: AudioFrameRange, to destination: URL) throws
Reuse existing snapshot/assetURL/export for full-byte original audio export. saveAudioDraft requires current revision==expected, input draft.revision==expected+1, fixed id/assetID; reject overflow. Exact UTF8 comparison protects external note/clip-name changes, including canonically equivalent Unicode, in verifyUnchangedManifest. Existing text protection unchanged.

Inspector checks regular no-follow/hardlink/path identities using existing safe helpers where suitable, bytes limit before reading, actual RIFF WAVE/caff and LPCM format using system AVFoundation, bounded full-frame decode validates truncation/nonfinite values, actual frame count/rate/channels/bits. SHA actual original bytes; waveform <=1024 buckets using chunked reads/cancellation checks. No whole enormous decoded array and no metadata-only acceptance of invalid payload. Source may move/change; import copies owned bytes before final validation/publication and compares identities, not reads source again after approval. Preserve original bytes, no extension-only trust. Existing selected-file same-volume RENAME_EXCL export protections reused; no sibling-temp assumption. Clip export explicit WAV float32 originalrate/channels, validates produced file/framecount before no-overwrite publication. Source/full original never converted.

Reservations: persist unique ID before recorder starts, return checked absent destination path in owned project directory. finalize only registered ID, validate closed file and create new original asset/audio document atomically removing reservation; on failure keep reservation/file available for retry. Do not add a scanner, cleanup API or auto-delete orphan files. import failures may leave owned unreferenced file, but never registered corrupted assets or lost originals; document exact recovery limits. If safe imports require private helper changes, confined to ProjectStore allowed file and preserve existing semantics.

Required tests (synthetic PCM/normal file system, no hardware): WAV/CAF supported and rejection of nonPCM, invalid/truncated/oversized/too long/nonfinite/rate/channel; waveform known extrema; Unicode/space paths; source exact bytes/hash and independence after source moved/deleted; import creates distinct document; schema3+older backup/identity preservation and future schema refusal; mismatched external manifest/backup failure; symlink/hardlink/collision no writes; draft range/IDs/revision/Unicode/overflow; capture reserve -> simulated CAF writer -> close/reopen pending -> explicit finalize -> saved original; failed capture retains raw; original export byte equality/no-overwrite; selected clip correct exact frame count and floating format, invalid range rejects. Extend existing test fixtures as local helpers in new tests; no dependency download. Lead will run CPU suites due known Worker SwiftPM nesting restriction.

## Shared fixed contract HUM1 / revision 1

Source base a406af9cd8908a77ae2f013d29121164d6fc0745. Execution base is the preparation commit recorded in the dispatch request, not the old source SHA. Shared concrete values are in Packages/UI/Sources/DWorkbench/Audio/AudioTypes.swift; Lead owns that file. No change to DInference, DRuntime, MLX, current model revisions/precision, signing, bundle ID, entitlements, Info.plist, dependencies/locks, scripts, source branch, user scheme or other task files. No network, model downloads, microphone permission request, actual recording/playback, GUI or full application build in Worker.

Product: short original audio import/recording, display waveform, normal playback/pause/seek, non-destructive named frame clips and notes, safe project save/reopen, original export and explicit clip export. No transcription, score, quantization, music/TTS model, mobile, plugin or broad metadata platform. Current PCM limit: actual WAV/CAF LPCM only, mono/stereo, finite sample rate8000...96000Hz, signed integer16/24/32bit or float32, nonempty <=120seconds and <=64MiB. Check both file bytes and decoded frame count; reject unsupported without converting silently. Waveform at most1024 min/max buckets. Range is half-open original frames, 0<=start<end<=frameCount; no byte/character offsets or beat quantization. Original file preserved byte-identical. Clip export is explicit WAV float32 at original rate/channel count, no resampling/normalization, never described as original-byte export. PCM32 integer -> float32 may lose low bits, originals remain intact. No playback/record operation requires MLX/GPU.

AudioDraftDocument.id == ProjectDocument.id, revision monotonic; assetID permanently references one original. Up to64 named clip ranges, unique IDs, selected ID exists or nil. Nonempty name <=256 UTF8 bytes; note <=16KiB UTF8, preserve exact Unicode bytes including combining characters. New original audio creates a new audio document, never overwrites current original or silently replaces image/text. No AI generation job fabricated for imported/human recorded audio; provenance importedFile/microphone, actual SHA256 plus actual audio format. Media metadata/source identity not inferred from UI labels. Audio bytes/project files are owned by app services, not views.

Recording reservations persist BEFORE starting capture. Pending captures are retained for explicit recovery on reopen; no scan outside exact registered paths, no guessing deletion. Normal stop/autostop/device failure reports once, closes/flushed recorder before import/recovery. Close/navigation while recording must stop+save (or remain open on failure), not drop callback or raw file. Cancel during permission wait invalidates late grant, no surprise recording. Original raw/partial files retained on failures; power-loss recovery not promised beyond persisted data and validated files.

CPU tests use synthetic locally generated PCM, no new downloaded fixture/third-party library. No original D or existing projects. Use only task output/tmp/cache and a unique directory per test; no default py_compile. If Python check needed use tokenize.open+compile(...,dont_inherit=True) in memory, do not execute target. Known nested SwiftPM sandbox failure: Worker must NOT run swift test or disable sandbox; Lead executes exact package checks after handoff. Optional direct installed swiftc -frontend -parse with module cache under task tmp only; any unexpected rejection pauses and reports, no invented fallback. This exception is declared before execution. Swift6 Sendable/actor correctness: no unchecked Sendable/nonisolated(unsafe) or broad catches to erase errors.

Only preflight until Lead explicitly sends IMPLEMENT with same spec revision and execution base. Return task/revision/run IDs, physical cwd/root/common Git dir/branch/fullHEAD, allowed path summary, forbidden scopes and test expectations. No writes in preflight. Lead independently verifies requested vs observed model/effort and workspace-write boundaries. Worker never commits (shared Git management is outside write roots), changes global docs/specs or spawns a child/background process. After implementation return changed paths, behavior, tests created/executed/not executed, explicit errors/events/remaining risks and own process state. Initial+max2 targeted repair rounds, one bounded Lead takeover only if applicable; new task number/model does not reset consumed budget. Every new instruction follows Lead review of preceding permission events and protected state. Ambiguous contract or need outside allowlist: pause relevant action and ask Lead before coding. No acceptance policy changes.
