# D-M0-01：可操作节点工作台

2026-09-25；spec_revision 1，contract_revision m0-v1。源基线 `58603870719a52ff07b6bb6e4d6d09e02c23901a`；隔离集成分支 `codex/d-m0-01`。用户本轮明确授权S0→S1→S2及S3小型扩展，不进入S4、不公开发布。历史“等UI方案”停点被本次附件M0范围替代，旧候选/许可/数据/预算保护不变。

## 依据和验收

按用户任务书v0.1、最小实施方案v0.1实施N01—N10和T01—T15；研究基线v0.3仅方向。原件及SHA256验证在 `D-Development/AgentTrials/D-M0-01/run-20260925T091107Z/inputs/`。三个markdown及README与提供的摘要一致；Launch Message未附，不影响明确用户指令。进度仅CURRENT_ACTIONS与本记录，不另建平台。

T01真实图文/确认/三候选/选择/尺寸格式/导出；T02无模型图片流程；T03同一短改写能力跨文字与画布；T04提交快照/项目及草稿隔离；T05必选、已连可选及类型阻塞；T06真实文字/图像取消与释放；T07等待点重开/确认幂等；T08局部重跑/旧输入；T09部分候选保留和失败项重试；T10输入与保存故障保护；T11未知版本不丢负载；T12独立小程序扩展；T13布局无计算副作用；T14透明转JPEG/不覆盖；T15准备后本地执行无云端。CPU/hosting/模型/GUI/真人分开，未验不称通过。

## S0与保护

真实源与上一回执一致；源仅scheme orderHint1→6未暂存，SHA256 `ca3635d88aa5a15397b90e528667c66c0e6db7e596176e885c79d194b544206c`；索引干净。完整diff/index/副本见本run start/protection。AP1 e5d24f4、CORE 9d3a503保持不接纳。检查Git共同目录、外盘挂载、hook/过滤/未完成Git操作后建立外盘独立工作树。原件、普通D、权重、旧工作树/证据保留，不stash/reset/restore/清理/改历史/main。

已核对ProjectStore是单一项目写入/锁所有者；现有文本候选只在内存，文字不是ProjectJob；现有ProjectSession部分入口读当前页，不能包装后冒称显式操作。M0新增图/运行/等待/资产来源值，仍由原Store提交。项目schema新值16明确跳过候选占用的13—15；仅源1—12及16可读，保留旧清单字节备份，测试项目验证，不批量迁移真实作品。未知workflow/node版本整图只读、保留完整快照，不以通用JSON编辑丢字段。

## 共享行为契约

操作静态注册、有版本参数与端口；实例UUID、布局、配置revision、运行快照、不可变资产版本/决定分开。配置为模块定义约束的有类型标量，不允许任意脚本或把说明字符串解释执行。媒体仅资产引用；同一Store保存图和资产，禁止新数据库或另建任务调度。图DAG按就绪串行，模型走同一个现有runtime；模型租约、取消drain/release不绕过。

| 情况 | 冻结行为 |
| --- | --- |
| 移动/缩放/折叠 | 仅布局，不修改配置revision、不加载或推理 |
| 新输入/改参数 | 新revision；旧结果保留并标旧输入，不自动执行下游 |
| 运行到这里 | 提交前显示执行/复用/等待；固定图和资产版本；人工等待停住并保存 |
| 重跑本步 | 仅用已就绪上游快照；不重算确认文字；新增attempt，旧结果保留 |
| 已连接可选口未就绪 | 明确阻塞，不当成未提供；集合不能自动取第一项 |
| 确认/采用 | 绑定具体waiting run及输入版本；重复确认幂等，预览/高亮不算采用 |
| 三候选部分失败 | 成功资产保留；失败项独立重试；部分集合需明确确认 |
| 保存失败 | 原作品和旧权威pointer不变；保留完成结果供保存重试，不自动重新推理 |
| 继续会话/改文稿 | 不改已发布资产版本，不自动运行图；画布文字可显式回文稿 |

首批图像尺寸模式fit/fill/stretch明确；PNG/JPEG编码，JPEG显式背景、质量，原件保留。导出显式用户目的地、不覆盖，媒体+元数据+回执；不保证所有文件系统故障下绝对一次。开发机不是模型/尺寸上限；本轮验证现有固定qwen2与Klein路径，不降既有精度。

## 执行与文件所有权

Lead负责共享Workflow值契约、ProjectStore/ProjectModels、ProjectSession与TextDraftSession的窄操作接线、运行协调、WorkbenchView装配、文档及组合验收。允许新增 `DWorkbench/Workflow/`、`UI/Views/Workflow/`、对应测试；具体Worker文件在派工前补充。既有DInference/DRuntime/MLX生产执行算法、Vendor、签名/权限、依赖锁不改。

写Worker沿受限独立CLI、固定base、独立物理工作树、只自身树/output/tmp可写，network=false，共同.git不授写；先PRECHECK，Lead核验turn_context后IMPLEMENT。禁止递归；初交+最多两轮针对性修复，再一次有界Lead接管；每轮15分钟。Lead审核异常/权限再发修复。构建/模型/GUI由Lead串行，Worker只获准局部静态检查；Swift编译沙箱不禁用。重要Lead差异由非实现者审阅。实际模型设置和未知服务端解析分开记录，不把模型来源当质量证明。

S1尽早普通签名隔离GUI，S2图文全链路；独立偏好/项目/缓存/产物，不关闭未知普通D。本人IME/解锁等进集中清单，其余持续推进。源只接纳已验组合固定SHA，前后核对scheme，显式暂存，普通工作分支推送沿有效授权。最终停M0检查点，未验收明确报告。

## S0准备时恢复点（历史，已被末尾替代）

S0进行中；集成树已建立，尚无产品代码/写Worker。源未动。接下来固定最小值契约、分派操作模块/界面，Lead接Store/运行；不等总体方案再实现。

## REGISTRY工作包（r1，派工前冻结）

Sol/high；仅新增`Packages/UI/Sources/DWorkbench/Workflow/WorkflowRegistry.swift`、`WorkflowBuiltins.swift`、`WorkflowExamples.swift`、`Packages/UI/Tests/DWorkbenchTests/WorkflowRegistryTests.swift`。读取同目录Lead已冻结的WorkflowTypes/WorkflowOperation，不改这两份契约/Store/Session/UI/文档/依赖。不执行构建、GPU/GUI/网络；只做无副作用读检查，Lead统一CPU编译。每轮900秒，初交＋2修复；停写消息回传范围/测试/问题/进程。

实现公开`WorkflowRegistry(operations: [WorkflowOperation]) throws`（拒绝重复ID）、`static var standard: WorkflowRegistry`（内置注册，不加载模型）、`definitions: [WorkflowOperationDefinition]`、`operation(_ id: String) -> WorkflowOperation?`、`validate(_ node: WorkflowNode) throws`（定义版本、未知字段/标量类型、专属限制）、`validate(_ graph: WorkflowGraph) throws`（唯一身份/连接，节点端口存在，无多源和环、类型兼容；草稿允许尚未连必选口，执行解析时再查）、`plan(_ graph: WorkflowGraph, target: UUID, only: Bool) throws -> [UUID]`（确定性祖先拓扑或仅目标）、`signature(_ nodeID: UUID, in graph: WorkflowGraph) throws -> String`（自身＋祖先配置/连接/资产绑定，忽略layout/name/graph revision，稳定排序编码SHA256）、`validateInputs(_ inputs: [String: WorkflowValue], node: WorkflowNode, connectedPorts: Set<String>) throws`（必选/已接可选不得缺失，实际类型再检，不取集合首项）。图/节点数量和输入文本有合理上限并明确报错，不将内存16G作上限。

标准ID和配置：N01 `d.text.input`字段text；N02 `d.asset.reference`使用node.assetReference，未选明确拒绝；N03 `d.text.template`字段template/inputText/otherText，命名输入input/other（均可选），仅`{{input}}`和`{{other}}`替换，实际引用且缺值报错，未知变量报错，不执行脚本；N04 `d.text.rewrite`可选input与fallbackText，instruction、maximumOutputTokens、maximumPromptTokens、temperature、topP，modelID是登记模型内容身份字符串（初始空=需明确绑定当前已登记模型后由Lead冻结）；N05 `d.image.generate`可选prompt/ref端口（prompt text/ref image），字段promptText/width/height/steps/guidance/seed/count/modelID（seed十进制字符串，count1...8独立候选）；N06 `d.text.confirm`必选input text；N07 `d.asset.choose`必选input images；N08 `d.image.resize`必选input image，width/height/mode（fit/fill/stretch），尺寸1...8192总像素<=32Mi；N09 `d.image.convert`必选input image，format png/jpeg、quality0...1、background white/black；N10 `d.asset.export`必选input text/image/images、fileName（单文件名，不含目录/..），目的地不保存在参数，来自明确运行服务授权。

所有输出port默认output；N01/03/04/06 text，N02 text或image（实际解析再查），N05 images，N07/08/09 image，N10 receipt。必选/可选以真实使用为准，N04/N05缺连线但fallback也空执行时拒绝；已有可选连线未就绪不可回退fallback。执行模块使用WorkflowOperationServices，注册/展示零I/O。普通节点执行只调用相应服务；N06返回reviewText，N07返回choose；N05全部返回有身份collection，不隐式选择。图像真正合法组合最终由现有capability再校验；本模块不可暗改精度/尺寸或声称新模型支持。文字默认instruction为英文简洁改写，token defaults128/2048、temp0.7/topP0.95；图像512/512/4/1/seed42/count3。其他模型专属真约束由服务处理。

`WorkflowExamples.text() / image() / file() / template()`返回可编辑WorkflowGraph，用上述公开定义创建节点和普通连接，不给执行器写示例专属分支；合理二维布局，text三节点、image完整八节点、file引用→resize→convert→export，template两文字输入汇合→template→confirm（字段示例体现命名）。N11移除空行留S3独立新增，不在此预写。

冻结测试：DAG正常/分叉汇合/拒环和双源；可选已连未就绪拒绝；集合接单值拒绝、实际N02类型不符拒绝；缺模板变量与脚本不执行；布局/标题变化signature不变、上游改值会变；未知operation/version只报错，不fallback；N05保留count/UInt64 seed；注册两个不同实现ID选择正确/no fallback。测试可使用自定义WorkflowOperation闭包与Services替身，不建替身调度器。

## IMAGE程序工作包（r1）

Sol/high；仅新增`Packages/UI/Sources/DWorkbench/Workflow/WorkflowImageProcessor.swift`及`Packages/UI/Tests/DWorkbenchTests/WorkflowImageProcessorTests.swift`。只处理现有ImageIO/CoreGraphics的普通图片，不调用MLX、不新增依赖。公开`WorkflowImageProduct: Sendable`含data:Data、mediaType:String、metadata:MediaMetadata、details:[String:String]，公开`WorkflowImageProcessor.process(_ data: Data, operationID: String, parameters: [String:WorkflowScalar]) throws -> WorkflowImageProduct`。ID为d.image.resize/convert；参数见REGISTRY。

PNG/JPEG实际格式嗅探、拒绝不支持/损坏/多页与过大数据（64MiB/32Mi像素/单边8192）；解码之前读尺寸检查，归一化EXIF方向与sRGB8bit，说明颜色转换并保留原件（输入Data不可变）。resize的fit在指定尺寸透明画布内等比居中，fill等比居中裁切，stretch直接指定尺寸；PNG输出。convert保持归一化尺寸，png保alpha、jpeg显式white/black背景和quality0...1；不能把透明黑默认冒充白底。实际输出回读格式/尺寸；details记录原格式、方向/颜色、输出编码、alpha、比例/背景策略，未知值明确unknown。主处理CPU/CoreGraphics，不是假称AI超分。

用内存合成夹具覆盖透明PNG→JPEG白/黑底像素（允许编码误差明确合理界限）、png alpha、fit/fill/stretch几何、EXIF方向、非ASCII不涉及路径、损坏/尺寸上限/非法参数。source Data摘要保持。不得删测试/放宽标准求过；不改其他文件，静态读检查后交Lead统一编译/CPU验收。相同受限CLI/900秒/初交+2修复规则。

## CANVAS工作包（r1，S1/S2表现层）

Sol/high，仅新增`Packages/UI/Sources/UI/Views/Workflow/WorkflowCanvasView.swift`和`Packages/UI/Tests/UITests/WorkflowCanvasPresentationTests.swift`；不改其他文件，不运行构建/模型/GUI/网络，Lead串行验收。受限CLI、900秒、初交+2修复。公开`WorkflowCanvasView(controller: WorkflowController, onTextModel: @escaping () -> Void, onImageModel: @escaping () -> Void, onImport: @escaping (UUID) -> Void, onDestination: @escaping () -> Void, onPublishText: @escaping () -> Void, onReturnText: @escaping (WorkflowAssetReference) -> Void)`。回调只由显式用户按钮触发，不访问文件/推理/项目状态。Lead实现控制器，Worker不新增替身控制器；按下列冻结接口写界面，编译由Lead后续完成。

`@MainActor @Observable public final WorkflowController`：`registry: WorkflowRegistry`；`graphs:[WorkflowGraph]`、`graph:WorkflowGraph?`、`selectedGraphID:UUID?`、`selectedNodeID:UUID?`（后两个可写）、`selectedNode:WorkflowNode?`、`runs:[WorkflowRun]`、`isRunning:Bool`、`isSaving:Bool`、`readOnlyReason:String?`、`errorMessage:String?`（可清空）、`progressMessage:String`、`textModelDescription:String`、`imageModelDescription:String`、`destinationDescription:String`。方法`latestStep(for:UUID)->WorkflowStepRun?`、`isStale(_ step:WorkflowStepRun)->Bool`、`preview(_ ref:WorkflowAssetReference) async throws ->Data`、`plan(target:UUID,only:Bool)throws->[String]`。同步编辑方法（内部报告错误）：`addNode(operationID:String)`、`deleteSelected()`、`copySelected()`、`moveNode(id:UUID,x:Double,y:Double)`、`setParameter(nodeID:UUID,key:String,value:WorkflowScalar)`、`connect(source:UUID,sourcePort:String,target:UUID,targetPort:String)`、`disconnect(_ connectionID:UUID)`、`undo()`、`redo()`；`canUndo/canRedo:Bool`。`addExample(_ name:String)`接受text/image/file/template（常规可编辑图，不专用调度）。异步公开方法内部捕获并显示错误：`save()`、`run(target:UUID,only:Bool)`、`cancel()`、`decide(stepID:UUID,accept:Bool,text:String?,candidateID:UUID?,acceptPartial:Bool)`、`retryFailedCandidates(stepID:UUID)`、`resume(runID:UUID)`。readonly时不允许写/运行；保存/运行状态要真实区分。

表现层：原生SwiftUI画布，左侧搜索/添加操作，中间可二维滚动/缩放/拖动节点/显示连接，右侧选中节点参数与输入输出检查，顶部流程选择/四个可编辑样例/保存/撤销重做/模型选择/发布文稿/明确选导出目录。连接交互最小可用为点击输出再点击输入；端口显示类型、必选/可选与连接。连线删除有明确菜单或列表入口。参数使用结构性field kind与当前标量，不能解析文案；模型ID字段只显示实际绑定字符串，用户用模型选择回调选择，不让自由字符串伪装模型。运行/本步按钮先显示plan中的执行/复用/等待确认，再明确按钮提交；没有后台自动运行。状态必须显示运行/等待/部分失败/保存失败/旧输入；错误就地可读，不只日志。

选中节点预览：text ref异步preview→UTF8只读；image ref→NSImage缩略；images逐项缩略/seed/错误，保留集合，不自行取首项。N06等待显示可编辑文字草稿，明确接受/拒绝；N07高亮和采用分开，部分集合必须额外勾选接受部分，不隐式采用。输入编辑期间不会直接修改发布资产。可以显示历史run摘要及恢复按钮，元数据/输入身份/摘要可查看。缩放/移动只调move或视图zoom；不启动任务。屏宽至少1100时三栏正常，窄时保留滚动/最小尺寸，不截断操作。使用原生材质/控件，避免新设计系统或第三方画布。

局部测试限纯表现逻辑/hosting已可见类型，不写假控制器或自行新契约；若控制器尚不可编译，报告所需检查由Lead组合后补验，不为绿色断言删边界。交付后回报未测试/阻塞/进程。

## S1实现与审阅检查点（尚未验收）

受限Sol/high初交REGISTRY/IMAGE/CANVAS已结束，实际并发及路由在本run对应request/process/turn_context索引。REGISTRY首交漏确认门且模板递归替换，Lead发出修复1：八节点A含确认、插入文字保持字面值、topP>0；修复已收，余1轮。当时IMAGE/CANVAS尚未消耗修复；之后CANVAS消费修复1，见下方。只读非实现者审阅Lead存储/运行增量发现关闭接线、决定并发、历史保存失败重算、重试身份、跨项目桥接及未知步骤版本/导入角色问题；Lead在原范围修正并补生命周期反例，审核发现保留，尚待组合测试和复审。没有把子代理静态自检当验收。

仅测试格式升级预期更新：原PitchWorkflowTests从v11备份后应到`currentSchemaVersion`，不再硬断言旧12；新增源12→16原字节备份及13—15拒绝测试。CPU首轮生产编译通过但旧12断言失败；后续新测试先后暴露访问级别和Sendable检查，已修测试/API签名，没有削弱运行标准。具体每轮日志、实际工作树摘要及命令保留在本run lead下，未提交组合不可凭HEAD单独复现。

方案明确：先新增显式“流程画布”视图入口，旧创作/资料仍直接用原服务、不强制经过图；用户主动发布文稿成为不可变资产，回文字时新建文稿。未知流程整份只读，原件不改。S1 GUI和真实模型尚未执行；不称M0完成。


## S1—S3 实施结果与复核（2026-09-25，M0尚未全验收）

### 代码、来源与修复

- `7536bc8eefba311d8983e4610c77da1f49c32980`：Lead接原Store/运行时/ProjectSession和真实SwiftUI画布；普通签名S1构建成功。
- `f73c53e222f19ee5b7510199d5c746db1484ffb4`：已发布结果保存重试、候选失败项身份/祖先来源、S3模块、实际模型测试入口。
- `aec1de436c87dbb58a30cd08791a17e1dda34876`：中断恢复冻结输入；首次绑定旧项目图片时核对已有摘要并保留参考图祖先，不把外部改坏的原件重新认成合法新来源。
- `a616126807ec20016c9db2d9061f05ec0dc1d9e5`：窄窗口viewport及稳定编辑器树；文稿/画布共享改写来源记录、执行时冻结backendID；真实ProjectSession两项目隔离及重绑/保存故障测试。该提交后的收尾代码和验证见下方最终补充，不把文档提交冒称新代码测试。

REGISTRY的初交682秒/修复1约197秒，IMAGE初交437秒，CANVAS初交627秒/修复1约33秒；三者实际实施时间重叠，证据为各目录implement-process.json而非角色名称。CANVAS修复`.offset(width:height:)`编译调用，余1轮；REGISTRY修复确认门/字面模板/topP后余1轮；IMAGE未用修复。Lead后来对画布viewport、元数据/现有资产菜单/折叠等作了实际补充，不把最终全部代码归给Worker独立成功。

请求/可观察设置均为 `gpt-5.6-sol/high`；受限CLI thread分别为REGISTRY `01a0d7dc-ee12-7562-8227-d53a15ce5d2c`、IMAGE `01a0d7dd-cdba-74e0-8304-ade0d370ad5f`、CANVAS `01a0d7e6-a05f-7eb2-9c37-8b97bfb9f30f`。工作目录/分支/基线/允许文件/输出/tmp/网络关闭与真实turn_context见各job/request/route-accepted。源树及共同.git未给Worker写根。REGISTRY一次拼错只读路径ENOENT、CANVAS一次补丁上下文不符均未观察到越界写入；审计保留。没有变更用户全局Codex配置。

非实现者`m0_storage_review`、`m0_execution_review`对Lead重要存储/协调增量静态检查；发现并修正关闭入口、await重入决定、失败保存误重算、重试输入、未知步骤、已有资产摘要/参考祖先和文稿来源丢失/重绑旧backend等问题。`align_layout_diagnosis`定位1100最小宽度与阈值换树丢草稿；真实hosting先失败后通过。复核摘要在`lead/final-static-reviews.json`，不是独立模型执行测试、GUI验收或密码学身份。

用量：本轮各CLI事件含逐次usage，可按task/run核对；本记录不把累计token快照相加，未重算历史。完整Lead消耗、隐藏服务端模型解析和订阅费用unknown；本批证明确实分工交付，不证明最省钱。

### 接线与数据责任

- `WorkflowTypes/Operation/Registry/Builtins/Examples`定义有类型值、DAG校验、输入组合、字面模板和模块静态注册。操作ID不是模型ID，实例UUID不是资产版本，布局不是配置。说明目录不参与执行解析。
- `WorkflowController`冻结图/输入/实现，显式计划和继续，人工等待持久化；旧输入不串新图，成功候选先保存再计算下一项；save-only恢复不重新调用模型。`WorkflowServices`使用原WorkbenchSession的同一engine/许可和TextDraftSession，排队模型使用权/drain/release不另造。
- `ProjectStore`继续是文件锁/manifest/资产单一所有者。不可变workflow snapshot先落盘，权威pointer最后提交；旧12原字节备份、候选13—15拒绝、未知workflow/节点/运行步骤只读原负载。新业务不是另一个数据库或平行项目系统。
- `ProjectSession.openWorkflow/publishTextToWorkflow/returnWorkflowText`接现有项目/模型准备和原生授权。文稿发布固定文字版本，返回建立新文稿。文稿改写候选也保存到同一资产库（不创建图），来源保存失败禁止接受但可保存重试；明确拒绝可丢弃尚未保存候选，不改原文。候选记录仅说明生成的替换片段，不宣称之后整篇人工修改都来自模型。
- `WorkflowImageProcessor`为普通CPU图片转换，AI图文模型仍使用GPU。透明JPEG白/黑底、几何模式、EXIF/sRGB8转换、实际回读被记录；原件不覆盖。导出是明确目录下原子不覆盖的媒体/recipe/receipt交接包，默认提示词withheld；不是PNG内嵌或C2PA。
- S3 `WorkflowRemoveBlankLines`只新增独立模块、一处注册和测试，未加Controller/Store特殊分支。第二实现的同端口/不同约束演练使用受控程序，经过真实Store发布并保留操作来源；另复用根DRuntime多backend选择/拒绝/取消释放测试。不声称新增生产模型、动态插件或任意图兼容。

### 已执行检查与失败链

证据根均为 `D-Development/AgentTrials/D-M0-01/run-20260925T091107Z/`。每个检查目录含cwd、命令、环境、时间、退出状态；未提交的中间轮不能仅凭日志HEAD复现，最终固定版本另验。

| 检查 | 版本/结果/证据 |
| --- | --- |
| 早期CPU | `cpu-first`旧schema12断言失败，改为真实currentSchemaVersion并新增12→16/候选13—15拒绝；后续生命周期访问级别/Sendable编译问题和失败项重复缓存均保留失败日志；没有删断言或降模型精度 |
| 完整UI package | a616126：`cpu-final`退出0；UI 108项/21 suites、ModelLibrary 23项/1 suite、DWorkbench 437项/60 suites。含宿主视图挂载和全部既有包回归，不是568次真实GUI操作 |
| 窄窗反例 | `hosting`及`hosting-fixed`失败于820宽时fittingSize1100；`cpu-provenance3`及最终完整回归通过。实际TextEditor待确认中文/emoji/组合字符经1320→820→1320保持同对象和草稿，未接受/推理。GUI输入法候选定位未测 |
| 运行时结构 | a616126：`runtime-final`根包70项/13 suites退出0，包含两个backend精确选择、拒绝不回退、drain/release交接和失败不混结果；不是两个新生产模型 |
| 最终普通App | a616126：`build-final-app`独立DerivedData-Regular、原开发签名构建成功；`ordinary-app-check.json`记录codesign verify退出0、沙盒true、无temporary-exception及当时三个关键文件摘要。没有替换普通D、改变源签名设置或把XCTest宿主交给用户 |
| 外盘宿主试验 | `real-models`退出65，ProjectStore.create EPERM，发生在模型前；不是模型失败或GPU内存不足。未扩大权限/重签 |
| 安全恢复 | `real-container`改为现有App容器专属M0Acceptance目录，小项目/结果之后复制到SSD；模型仍SSD只读。aec1de4+记录过摘要的测试入口变更，真实链通过87.3秒；不取代最终记录修补版验证 |
| 最终真实图文 | a616126，`build-final-tests`构建成功；`real-final`退出0，1项串行集成测试85.481秒。Qwen revision a5339a4131f135d0fdc6a5c8b5bbed2753bbe0f3、Klein revision ef52ee019fd1d0e75ae4deb40476ba65989716d7；512×512/4步/guidance1，三候选seed42/43/44，明确测试驱动确认/采用，再384×256/JPEG导出回读、保存恢复 |
| 真实取消 | 同一real-final内：观察文字增量后取消，观察图像去噪phase后取消；终态cancelled、active与queue空，约0.030/0.088秒。是该边界样本，不承诺任意GPU操作瞬间终止；后续任务仍在同一runtime依次运行 |
| 文稿/画布真实对照 | `real-final-artifacts/real-session-result.json`：实际ProjectSession文稿入口、相同请求/模型/backend，接受前原文不变；接受并重开来源仍在、hiddenGraphCount0。非人工GUI点击 |
| 真实产物 | `real-final-artifacts/`及同名.json逐文件摘要，保留原项目、三候选、JPEG包、recipe、receipt；原容器专属测试目录也保留，不删用户内容。结果样图肉眼为正常室内场景；这不是专业审美/像素确定性承诺 |

离屏界面证据：`lead/render-final/M0-offscreen-hosting.png` 为a616126实际SwiftUI视图树，不是绘制的设计稿；`hosting-render-final`又执行同一hosting回归通过，仅为产生可交付渲染，不叠加总通过率。一次外部运行器的输出目录与渲染目录重名，在启动测试前被FileExistsError拒绝；改用不同日志目录重试，未覆盖旧证据。

环境：M4/16 GiB开发机，macOS26.6.2(25G83)、Xcode27.0(27A266a)、Swift6工具链；模型精度、权重revision和依赖锁未变，无本批下载。

完整验收不采用“所有测试数相加=产品通过”的算法。MLX原有编译器unused常量warning、XCTest宿主WindowSnapshot garbage等日志没有被包装成GUI成功；本轮没有改底层数值/精度或权重。

### T01—T15 分层状态

更新到2026-09-25本人解锁补验停点：R为本任务run-20260925T091107Z；G为run-20260925T112546Z-gui。18cb9ec真实全链与c8ae6f0取消增量证据分别标记，不宣称整个全链在新提交上重跑。除T15外，下表范围内已有相应验证；各项限制仍保留。

| ID | 当前证据 | 未验证/限制 |
| --- | --- | --- |
| T01 | 18cb9ec `real-delivery` production App factory真实全链/JPEG通过；G普通沙盒App原生改写→明确接受/恢复→真实3图→采用seed43→尺寸/PNG→导出通过 | 主链原生证据在18cb9ec；c8仅取消收尾增量，不改模型数值。不宣称作品质量保证 |
| T02 | 真实Store/ImageIO CPU且engine零调用；G原生导入、端口重连、384×256/JPEG白底、目录选择和导出通过 | 不代表RAW或任意色彩管理 |
| T03 | 相同TextDraftSession，真实ProjectSession/画布请求及来源对照；G原生独立文稿改写/接受/发布后继续编辑，发布快照不变、无隐藏图；另一个模板组合可编辑并拒绝 | 不宣称新聊天平台或小模型写作质量 |
| T04 | 同Store换图和真实ProjectSession A→B drain的CPU反例，A提交快照/A新草稿/B项目严格区分 | 未用真实模型同时切项目；按设计仅一个重任务 |
| T05 | 真实registry必选/已连可选、错误类型、集合不隐式单选、版本/实现缺失拒绝；G原生断开/重连、端口及元数据可检查 | 原生操作未穷举每一种拒绝，拒绝反例由CPU覆盖 |
| T06 | real-delivery真实取消/交接；c8完整CPU及普通App原生图像取消→显示已停止/释放→下一真实文字任务完成，取消步骤无原始CancellationError | 非所有加载阶段新实验；磁盘终态本身不能独立证明GPU释放 |
| T07 | 等待/并发/过期CPU；G原生未接受中文/emoji/组合字符草稿保存、正常退出/重开保留；不自动接受/生成 | 未做OS强杀恢复或真人IME输入 |
| T08 | 提交后编辑/局部重跑等CPU；G原生仅重跑尺寸，三候选身份/版本/摘要不变；后续旧输入可见，明确执行后新增384×256导出包 | 不把局部重跑写成所有节点缓存命中保证 |
| T09 | 三项中一项受控失败、成功保留、仅失败重试、部分确认；历史保存失败不重复生成CPU | 模拟失败不等于实际故障硬件上3项实验 |
| T10 | 原件摘要改变拒绝、发布/历史保存故障注入、不覆盖、重试不重算CPU | 未物理拔盘/填满磁盘；基线rename后fsync失败有“提交不确定”边界，下文说明 |
| T11 | 源12备份/16往返、拒13—15、未知archive/graph node/run step负载保留只读 | 未迁移真实用户项目，无批量迁移 |
| T12 | N11真实Store/Controller CPU，小模块/注册即可接入；双程序约束与来源演练 | 非新模型性能或插件热加载 |
| T13 | hosting宽窄/编辑器/待确认草稿及无隐式决定通过；G原生拖动、折叠、连接、窗口宽窄/滚动；c8原生缩放100%→70%→100%并保存，运行/资产未变 | CUA原生操作不是用户手工IME专项，H22仍按决定延后 |
| T14 | 透明PNG→JPEG白/黑背景、几何/EXIF/编码回读、不覆盖CPU；G普通App真实目的地授权，文件JPEG384×256/图文PNG512×512/局部重跑PNG384×256三包回读 | 未物理拔盘或磁盘填满；不冒充任意专业格式验收 |
| T15 | 已有本地权重及offline flags、无云推理/下载接线；实际断网验收**未执行** | 附加网络拒绝导致App启动失败；两次人工断网等待均未触发测试。第一次路由器WAN断网与本机接口检测不符。无实际断网流程证据，不计通过 |

保存边界澄清：前面冻结表中的“旧权威pointer不变”适用于发布前失败。原ProjectStore的rename成功后若fsync报错，磁盘可能已提交而内存仍旧；本批保留已发布文件、明确错误且重试可报告外部变化，需重开核对，不承诺所有文件系统异常自动回滚或绝对一次。此为已记录基线限制，不偷换成所有磁盘故障测试通过；未为M0重写公共commit。

### 最终确认草稿收尾

- `bc020ba1a243ee5c502df72e55f1711ea522d282` 和 `18cb9ec9cdf81a7e849204a40226cc91223c275e`：未接受的人工确认草稿纳入既有运行记录，保持旧记录兼容，不发布/不启动。关闭后/另一图迟到回调不改错步骤；等待草稿可在另一任务计算时编辑，但接受/拒绝仍受空闲门槛约束。
- 非实现者复核发现异步原文读取覆盖刚输入草稿、运行期间写入拒绝与可输入不一致，以及读取失败把拒绝也禁用；均在本批窄修。最终复用已有原生文字组件，旧调用默认标识不变、marked text 内容与回调归属保护不改。受控延迟与组字后开始另一执行→提交中文→显式保存均有hosting反例；这不是系统输入法真人验收。
- 初始新增hosting对SwiftUI内部`isEditable`、AXEnabled的推断不成立，失败日志`draft-boundaries`、`draft-ax`等保留；没有删除禁编辑/恢复条件，最终测试直接检查实际使用的原生编辑器。一次Lead局部替换误命中候选重试守卫，经Lead与Reviewer发现后在提交/真实模型前恢复，最终该方法保持原串行所有权。
- 审阅：`lead/draft-static-review.json`，只读非实现者检查，未另行执行测试。Lead承担本轮修补、组合测试及构建，Worker原始贡献和修复次数不变。
- 最终完整CPU：`18cb9ec9cdf81a7e849204a40226cc91223c275e`，`cpu-delivery`退出0，UI109项/21 suites、ModelLibrary23项/1 suite、DWorkbench438项/60 suites；根运行时70项复用a616126的`runtime-final`，相关Sources/Tests/Package零差异经`runtime-evidence-reuse.json`核对，不宣称重跑。

- 最终实际模型：同18cb9ec `build-delivery-tests` 构建通过，`real-delivery` 完整进程退出0（88.582秒），包含1项真实集成测试；既定revision/精度和512×512三候选不变，实际JPEG回读、恢复、取消与ProjectSession真实改写对照再次通过。图文生成与取消部分84.077秒；文字/图像取消释放样本0.030/0.099秒。`lead/real-delivery-artifacts/`、摘要清单及`.xcresult`保留，所有报告仍明确`guiValidated:false`。

- 交付普通App：18cb9ec `build-delivery-app`退出0（20.723秒）；`ordinary-app-delivery-check.json`记录完整性通过、既有沙盒开启且无测试临时例外，以及D可执行文件、D.debug.dylib、Info.plist、CodeResources四文件摘要/大小/时间。`lead/启动M0试用.command`指向这个重建产物，独立偏好ID不变、检测任何D运行即拒绝重复启动；zsh语法检查通过，原生启动尚未执行。
- 最终视图证据：`lead/render-delivery/M0-offscreen-hosting.png`，18cb9ec实际视图树离屏渲染；`hosting-render-delivery`退出0。只为产出对应版本图像，未再次叠加测试数、未称桌面截图。
- 新上下文只读接手：`readiness_fresh_handoff`从AGENTS/CURRENT_ACTIONS进入后指出四项试用入口说明与产物版本同步，均已修正；其余T01—T15边界和停止点可恢复。未参与实现、未代Lead执行测试。摘要在`lead/fresh-handoff-review.json`。

### 8446c101交付时恢复点（历史，已由下文更新）

源HEAD `58603870719a52ff07b6bb6e4d6d09e02c23901a`不变；候选受测代码/App `18cb9ec9cdf81a7e849204a40226cc91223c275e`，最终仅四份文档提交自身SHA写`lead/final-handoff.json`。三个Worker停写；只读审核完成，重型操作由Lead串行。H25锁屏/普通App外盘授权/原生流程/断网验证未关闭，不能称M0完成；交付可运行候选与[试用说明](../M0_TRY.zh-CN.md)，先不把新路径推进源。本人回归后沿同一任务补验，不重新派工或重置预算，不自动进入S4。


### 2026-09-25 本人解锁后的 H25 集中办理（部分关闭）

本轮证据 `D-Development/AgentTrials/D-M0-01/run-20260925T112546Z-gui`（下称G）；旧R仍为run-20260925T091107Z。起点候选8446c101、源5860387完整值见前文，个人scheme摘要/未暂存/index与开始一致。原生普通App18cb9ec四关键文件前后不变，结束后完整副本保留G/apps/D-M0-18cb.app；不是XCTest宿主。正常退出三个本轮实例，未结束普通用户App。

- 普通沙盒实际选择器：新建G/projects/M0现场验收.dproject、导入透明PNG、选择两个已安装模型目录和G/exports。没有新密码/TCC请求；Codex文件访问不替代App自己的目录授权。
- 文件组合：手动断开/重新连接端口，384×256透明PNG转JPEG白底并导出；回读真实编码/尺寸。保存、正常退出重开，资产/流程恢复而不自动执行。
- 图文原生完整链：真实改写后等待，图像尚未生成；中文/emoji/组合字符未接受草稿经窗口宽窄、折叠、拖动、保存退出重开保留。明确接受后仍不生成，点恢复才运行。三候选seed42/43/44，选择43，高亮与采用分开；明确继续后处理/导出。来源面板实际展示完整模型revision、输入文字、seed43、512×512/4步及上游身份。
- 局部重跑：尺寸改384×256，显式只重跑该步；三模型候选ID、版本、摘要不变，后续显示旧输入。再明确执行转换/导出得到新包，旧输出保留。注意图文流程实际默认导出PNG；JPEG变换由文件流程和既有真实测试另证，不混写。
- 独立文稿：真实选择改写、接受/保存、显式发布快照后继续修改正文，已发布资产未改变，图流程没有隐式运行；双输入模板编辑中文并执行到确认，再明确拒绝。普通文稿不需要隐藏图。
- 本轮CUA的Unicode合成按键曾丢字符，改用真实原生编辑器AX值写入并核对持久内容；这是自动化输入方式差异，不作为真人IME通过/失败。H22不重开。原生截图/AX在本会话CUA记录；离屏截图不冒充原生证据。
- 控制器取消提示缺陷：真实图像取消已终态且下一组三图成功，但横幅残留“正在取消”和系统CancellationError。Lead局部修补仅WorkflowController与直接测试，状态在finish释放后转已取消；保存失败仍优先，resume准备期取消不被旧run状态挡住。新回归先失败6断言，再通过；R/lead/gui-cancel-before、gui-cancel-after、gui-cancel-combined保留。非实现者m0_storage_review查出准备期回归，修补后再次只读确认；不是独立模型实测。代码提交`c8ae6f0a94958f494c4648a2beed547e7a98f0a4`；完整UI包109/23/439通过（gui-final-cpu），最终build-for-testing通过；无模型数值/依赖/签名配置改变。
- T15先尝试进程网络拒绝：同一exec链探针EPERM得到证明，但普通沙盒App启动SIGTRAP，未完成离线流程，不改权限/签名绕过。随后一次用户断路由器WAN与工具“本机接口离线”检测不匹配，10分钟等待后无测试启动。路由器断WAN是有效离线方式，不能把检测条件误当产品要求；这次只能证明测试未启动，不能追认运行通过。第二次明确要求本机Wi-Fi关闭，等待也在12:29:33 UTC结束、testPID为空；不推测未观察到的本人操作。结束提示音进程exit0不证明用户听到，现无等待进程。失败/未执行证据和用户说明均保留。

- 新版普通App：`gui-final-build-app`在c8ae6f0完成，既有签名/沙盒未改，四关键文件前后相同。原生图像取消run `16E88FE6-4FFD-4AF3-B36E-C1279E210CE4`终态无错误；GUI显示“已取消，计算已停止并释放资源。”，随后真实文字重跑`FB5B742E-F226-460F-A59A-3559BE3B1F4A`完成。缩放控件经原生AX从100%→70%→100%，截图确有布局变化，没有新增执行；已保存并正常退出，持有的session79949完整exit0。真实取消增量通过，不重复记一次完整三图全链。
- 独立只读核对：m0_execution_review核对最新manifest revision108及所指快照摘要、取消/后续文字步骤和两次未执行状态；未代Lead跑测试或从磁盘推断GPU释放。Lead另对实际媒体字节校验三旧候选、发布文字和旧图像文档不变；`c8-native-final-check.json`、`c8-native-final-project.json`和`c8-ordinary-app-after.json`保存对应事实。

收尾恢复点：源仍`58603870719a52ff07b6bb6e4d6d09e02c23901a`，个人scheme内容/摘要/未暂存/index不变，源索引干净；候选受测代码`c8ae6f0a94958f494c4648a2beed547e7a98f0a4`，后续本地提交仅本任务四份文档，提交自身SHA写G/evidence/final-handoff.json，不冒充在文档SHA重跑。所有本轮自有测试/构建/等待及原生App已结束，只读审阅结束；候选与证据保留，未推进源、未推送。下一动作只补T15：重新协调一段明确断网窗口，复用已构建production宿主及原冻结测试，全程记录断网、退出与产物，再决定接纳；不要沿用已过期的等待提示。其余当前集中操作已处理，H22按用户决定延后。未来人不在/锁屏/设备或本人点击的阻塞继续追加集中清单，不重开已关闭旧项。M0尚未全验，不进入S4。


## 2026-09-26 T15集中补验与M0接纳

状态更新：**T15在已整合节点边界的生产代码76bd5963c779c74268c69fa6a4851533768ea4c3上通过；M0已作为Boundary祖先快进纳入本地源。** 原M0候选bbf508024a80250b9e4b468e1f5713b7b13dcc13和上表历史证据保留，不把此次结果写成旧SHA重新执行。

证据`D-NODE-BOUNDARY-01/run-20260926T073533Z-acceptance/offline-confirmed`：本人关闭本机Wi-Fi后，冻结生产集成测试实际1项通过，101.070秒；包含真实文字、Klein三图、人工决定的测试驱动、尺寸/JPEG导出、保存重开、实际执行后取消/释放和独立文字会话对照。100次执行/结束采样均离线；路由无重连记录。产物/来源/哈希持久留外盘。CPU、原生M0历史、Boundary原生与此次断网测试各有来源，不相互冒充。

第一次本轮包装仍未触发，已记录系统措辞/route退出码错误并只修外壳，保留失败；非实现者复核r2后通过。此前WAN断开和其他未启动证据不追改。T15只证明准备好本地模型后的冻结流程可离线，不是干净机安装、全模态或零网络调用证明。

M0 T01—T15在各自声明的有限范围已有验收证据；原表的强杀/物理故障/发行等未测限制继续存在。当前源/完整受测与最终版本只见CURRENT_ACTIONS、本次final-receipt；本地接入SHA为15ee8495deb4ff0da10a62ae03c05cbf890278f7，后续结案仅文档。用户要求本轮同步后审计，不进入S4或自动公开发布。
