# 历史失败与权限审计

日期：2026-09-07。源码基线 `86720f9`。本次只检索、核查和整理文档，没有修改系统授权、应用签名或生产代码，也没有重新运行模型。

结论：找到的历史权限阻塞，相关开发／测试任务后来已有成功证据；没有发现仍须补跑、却一直被权限挡住的已批准核心验收。仍有待处理的授权稳定性和发行前验证，见下表。**历史成功不等于已经确认今天系统设置中的 D 开关状态。** 用户报告今天出现 D 外盘授权，本次没有读取或改变该开关。

## 检索范围与判定方法

- 按 `权限 / 授权 / 未执行 / 阻塞 / 重试 / 失败 / 超时 / 跳过 / EPERM / EACCES / denied / failed / error / skipped` 检索阶段报告、机器可读证据、日志，再读取命中位置。
- 检索本机 9 月 6–7 日的 14 份相关会话文件中的可读用户沟通记录，包括主会话和相关协作任务；主会话 ID 为 `01a075e2-251e-7fc2-9bbd-762ff02227a1`。历史工具拒绝另核对原始输出。
- 日志扫描覆盖 `D-Development/Logs` 下 485 个 `.log/.txt/.json/.md` 文件，排除 xcresult 内部二进制数据库与附件目录；结构化计数使用既有导出摘要及阶段证据。未把全部会话载入上下文，也未重解包每个历史 xcresult。
- 同一失败的多次转述合并为一项；保留具体失败轮次，不把编译、CLI、CUA 操作和 XCTest 混为同一次通过。未证实的根因保持未知。

以下日志路径均相对 `/Volumes/CodexProjects/Codex/D-Development/Logs`；这些大日志保留在外盘，摘要与处理状态保存在仓库。历史报告继续保留当时事实，不改写旧失败为成功。

## 权限或执行环境相关操作

| ID / 操作 | 当时失败原因与证据 | 后来是否成功 | 当前处理 |
| --- | --- | --- | --- |
| A01 GitHub 推送 | 9 月 6 日首次推送检查没有可用 GitHub 登录凭据；主会话第 739、824 行。读取公开仓库不等于拥有写权限。 | 浏览器授权后主仓库及原子模块推送成功（第 878 行）；最新实施提交 `86720f9` 也已核对远程。 | 已解决；沿用现有凭据，无需再提供 token。后续认证过期再按实际错误处理。 |
| A02 完整 MLX XCTest 启动 | `TestRunnerAttempts/20260906T122517Z-before-retry/mlx-tests.log` 记录测试 bundle 加载前 runner 超时。`xctest-permission.log` 明确记录 `SystemPolicyRemovableVolumes`，访问者 `com.apple.xctest`，提示归属 Xcode。不是 D 的图像执行失败。 | 两次后续文本 MLX 运行成功，最终 25 声明／37 场景、0 跳过；图像 B2 扩展后 61 声明／120 场景通过。见 [基础验收](FOUNDATION_STAGE_ACCEPTANCE.zh-CN.md)、[B2 验收](IMAGE_RUNTIME_ACCEPTANCE.zh-CN.md)。 | 已补验；无需因旧检查点仍写“等待授权”重复判为欠账。再次出现时确认实际被提示的是 Xcode／runner，给 D 授权不能代替它。 |
| A03 自动点击系统权限提示 | 主会话第 1339 行工具原文：`Computer Use is not allowed to use the app 'com.apple.UserNotificationCenter' for safety reasons.` 当时无法通过该工具访问承载提示的系统应用。 | 没有把这次工具操作记为成功；后续用户授权后测试可以运行。 | 这是工具的明确安全拒绝，扩大 D 权限不能解除它。遇到同类系统授权仍由用户完成，不换工具绕过拒绝。 |
| A04 XCTest UI 启用自动化模式 | `workbench-ui-tests.log:514` 附近记录 `Timed out while enabling automation mode`，3 项用例未执行。**日志不足以确定是外盘、辅助功能授权还是 runner 初始化故障**，不能根据“automation”一词认定具体权限缺失。 | 模型库阶段 6/6 UI 测试实际执行成功；探索阶段 8 个不同场景最终均通过。原有新建／面板／欢迎页流程包含在后续测试中。 | 旧启动阻塞已不再阻止验收；若复现先分辨“未启动”和“断言失败”，保存 runner 诊断，再检查被提示进程。 |
| A05 D 已选项目位置仍无法新建／访问模型或产物 | 安全遍历错误地要求读取每一级祖先目录；用户授权子目录不保证能枚举其父目录。`O_EVTONLY` 候选也失败；受控试验与 POSIX 权限对照均保留。 | 改为祖先只请求 `O_SEARCH`，最终授权根目录仍严格检查；31 声明／62 场景通过，普通沙盒实际新建、生成、重开通过。 | 已修复实现。见 [目录访问调查](research/SANDBOX_DIRECTORY_ACCESS.zh-CN.md)。不要求用户用全盘授权掩盖代码问题。 |
| A06 UI runner 创建探索夹具 | `exploration-ui-tests-1.log:880`、`:887`：runner 在外盘创建 `Exploration.dproject/Tasks` 遭 `NSCocoaErrorDomain 513`／底层 `EPERM`。这是测试准备失败，尚未进入 D 的创作流程。 | 两个微型 PNG 改在 runner 自己的临时目录准备，通过原生打开面板给 D 访问权；第 6 轮两项流程通过，第 7 轮整理／重开复验通过。 | 已修复夹具位置；继续保持大模型、构建和证据在外盘。给 D 授权不自动赋予测试 runner 相同目录访问权。 |
| A07 新位置 PNG 导出 | 正常 D 的 NSSavePanel 只授予目标文件访问权；旧实现在旁边创建临时文件被拒绝。较早导出成功不能证明这种较窄授权情形正确。 | 系统同卷临时目录准备后，以 `RENAME_EXCL` 原子发布；受控权限试验、失败边界测试及正常应用新目标导出均成功，摘要与原作品一致。 | 已修复，包含在 `86720f9`。见 [探索验收](EXPLORATION_STAGE_ACCEPTANCE.zh-CN.md)。新增目的地仍通过保存面板选择。 |

## 其他原因造成的失败与缺陷

| ID / 操作 | 原因 | 重试／修复结果与依据 | 当前处理 |
| --- | --- | --- | --- |
| B01 原项目首次构建 | `AttentionBlock.swift` 中 `sqrt` 的名称解析选中 MLX 运算，标量与数组类型不匹配；`build.log:7838`。 | 显式使用 `Darwin.sqrt` 后 `build-fixed.log` 成功；后续正常应用持续构建通过。见 [初次审阅](PROJECT_REVIEW.zh-CN.md)。 | 已解决；与权限无关。 |
| B02 并行构建依赖解析 | 旧应用与 MLX 集成共享可变 SwiftPM checkout，解析／检出相互竞争；主会话第 2220 行。 | 分离 `SourcePackages-App` 与 `SourcePackages-MLX`，普通远程 clone 独立构建也通过。见 [MLX 修复记录](MLX_OWNERSHIP_FIX.zh-CN.md)。 | 已解决；保留独立 checkout，不能重新共用可变目录。 |
| B03 MLX 重复加载的持续分配增长 | 上游 C++ array 赋值覆盖未清理 sibling 引用关系；旧实现每轮约 2.7 KB 增长，原版底层对照 7 组中 2 组失败。 | 固定源码最小补丁；7 组／36 检查通过，50 轮真实文本推理释放后的活动分配与缓存均为 0。见 [所有权修复](MLX_OWNERSHIP_FIX.zh-CN.md)。 | 已解决已复现路径；保留升级回归，不推论任意模型永不泄漏。 |
| B04 运行时终态重入 | 终态交接中的 actor 重入窗口会产生不合法 active／queued 身份重叠；`terminal-reentrancy-original.log` 实际失败。 | 状态转换移入无 await 区段，对照和最终 17 项核心测试通过。见 [基础验收](FOUNDATION_STAGE_ACCEPTANCE.zh-CN.md)。 | 已解决；保留确定性门控与身份检查。 |
| B05 图像实验约 0.5 MiB 残留 | B1 只测到 524,304 B，初期没有完整归因；不是授权失败。 | B2 独立对照确认为 attention mask 缓存 524,288 B 加全局随机状态 16 B；补显式缓存释放及任务独立 RNG，正式重复生成释放归零。见 [B2 归因](IMAGE_RUNTIME_ACCEPTANCE.zh-CN.md)。 | 已解释并修复正式路径，不采用放宽残留阈值。 |
| B06 CPU 夹具路径错误 | `model-library-fixtures-1/2.log`、`model-library-fixtures-all-5.log`：系统 `/var` 临时路径经过符号链接，被路径保护拒绝；第 3 轮另有 URL 表示比较差异。 | 测试自有系统临时目录先规范化；路径断言比较语义路径，生产层的未知符号链接拒绝保留；第 6 轮 55 项、后来 58 项及最新 82 项通过。 | 已修复测试前提；不是放宽模型／作品安全检查。 |
| B07 暂停下载测试断言 | `model-library-fixtures-4.log:33` 把服务器延迟写出的结束事件也当成重开后的新请求。 | 断言改为统计 `start` 事件，仍验证暂停后不自动发起下载、继续时从持久位置接续；后续完整回归通过。 | 已解决测试观察方式；真实下载暂停／退出恢复另有实测。 |
| B08 UI 选择器、可访问性和编辑状态 | 模型库初轮 3 项因原生面板 label 不符失败；探索阶段又发现 List 合并按钮、父标识覆盖子项、控件未滚入视野、NSAlert 实为 Dialog，以及异步状态过早读取。首次命名弹窗还存在产品快照不同步。 | 对照真实辅助功能树，分别修复产品按钮结构／弹窗状态和测试选择器／等待条件；保留原断言。最终 6 个原有场景与 2 个新场景均实际通过；命名修复后重跑相关流程。 | 已解决对应路径。8 个场景通过来自不同轮次，**尚无最终同一结果包中 8/8 全绿的记录**；稳定签名改动后归并执行一轮。 |
| B09 v2 中间编译失败 | `project-v2-build.log:21`：初始化完成前闭包捕获 `self.jobs`。 | 初始化逻辑修正后最终工作台 82 项测试、正常应用构建成功。 | 已解决的开发中间态，无需权限。 |
| B10 模型安装测试一次偶发失败 | `exploration-workbench-before-fixture-readiness-fix.log` 中旧模型库恢复测试未达 installed 状态；**原根因未证实**。 | 31 次定向及一次完整复验未复现；独立发现并修正端口文件就绪竞态，最终标准脚本 82 项通过。 | 保留观察项。不能声称竞态就是原失败原因；再次出现先保留安装错误、HTTP 记录、端口和临时夹具，再修复，不无限重跑掩盖失败。 |

审阅中发现、随后修复的旧句柄误取消、关闭期间准入、模型重新定位失败回滚、移除中断恢复、草稿写入竞争等，已归入上述阶段生命周期测试；没有据此登记额外的“尚待权限”任务。已退出主链路的旧 SD／SDXL、SD3 占位、旧下载暂停和聊天入口问题，不属于本次权限补验清单；若未来复用，需要重新进行模型／场景验收。

`PNG read error`、损坏 SHA、写盘拒绝、断管及取消退出码，有一部分来自**主动故障注入并断言正确拒绝**的用例。仅搜索 error 会误把通过的负向测试当成失败，必须结合用例结论和最终摘要。AppIntents 无依赖的 metadata skipped、未使用依赖、可选 CUDA 排除路径及早期调试缓存路径警告，不表示核心测试被跳过；已知构建警告暂不阻塞本机开发，但在依赖升级／发行检查中整理。

## 当前待办：哪些是真的没有完成

| 项目 | 状态与原因 | 处理时机 |
| --- | --- | --- |
| D 稳定开发身份与跨构建授权 | 当前 Debug 为 ad-hoc，`TeamIdentifier=not set`。这是重复提示的已知风险，**没有证据认定它就是今天提示的唯一原因**。本机发现 1 个有效 Apple Development 签名身份，但尚未用它构建 D 或验证钥匙串访问。 | 新产品工作包前优先处理 D-C01a；先稳定签名，再集中授权和做跨构建重开验收。见 [操作方案](MAC_PERMISSION_SETUP.zh-CN.md)。 |
| 今天 D／Xcode 的系统授权开关 | 历史外盘操作已成功；今天的开关状态未核实，用户目前不在 Mac 前。 | 用户回到 Mac 后按方案检查。今天不制造新的系统提示，不重复请求无法完成的授权。 |
| 一次完整的最新 8 项 UI 测试 | 各场景已分轮通过，无遗漏的单项；最终合并运行尚未记录。 | D-C01a 的构建身份变化后运行，随后恢复正常签名再检查实际应用。 |
| B10 偶发测试根因 | 复验通过，原失败未定位。 | 保留诊断；下一次标准回归关注相同测试，复现再定向调查。 |
| 实际拔盘、物理断电／强杀、真实容量耗尽 | 尚未执行；既有检查通过自有目录权限、路径失联和故障检查点模拟，不等价于真实设备事故。 | 可靠 Alpha／发行前使用专用验证副本、测试卷或磁盘映像设计工作包；不得拔出正在承载整个工程的共享 SSD。并非缺一个授权开关即可补齐。 |
| 完整 VoiceOver、硬件／屏幕矩阵、Release 长时与发布升级 | 目前只有本机基础矩阵及限定推理证据，未承诺完整发行。 | D-U01／D-C01 发行门槛；新的模态与参数按目标表逐场景验证。 |

本次审计不重新宣布历史测试通过，也不增加新的推理测量。最新实现验收仍以 [探索阶段报告](EXPLORATION_STAGE_ACCEPTANCE.zh-CN.md) 和 [机器证据](research/EXPLORATION_EVIDENCE.json) 为准。
